---
phase: 01-core-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - gsd-runner/src/session-runner.ts
  - gsd-runner/src/config.ts
  - gsd-runner/test/session-runner.test.ts
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04
  - LOOP-03

must_haves:
  truths:
    - "Session runner invokes Agent SDK query() with configurable maxTurns"
    - "Session runner tracks compaction events in-stream and flags when threshold is hit"
    - "Session runner starts a fresh session (new query call) for resume, not SDK continue"
    - "Session runner accepts an AbortController for graceful cancellation"
    - "Session runner sets permissionMode bypassPermissions on every query"
  artifacts:
    - path: "gsd-runner/src/session-runner.ts"
      provides: "Agent SDK wrapper: runGsdCommand() function"
      exports: ["runGsdCommand", "SessionResult"]
    - path: "gsd-runner/src/config.ts"
      provides: "Configuration loader from environment variables"
      exports: ["loadConfig"]
    - path: "gsd-runner/test/session-runner.test.ts"
      provides: "Unit tests with mocked Agent SDK"
  key_links:
    - from: "gsd-runner/src/session-runner.ts"
      to: "@anthropic-ai/claude-agent-sdk"
      via: "imports query function"
      pattern: "import.*from.*claude-agent-sdk"
    - from: "gsd-runner/src/session-runner.ts"
      to: "gsd-runner/src/types.ts"
      via: "imports RunnerConfig, SessionResult types"
      pattern: "import.*from.*types"
    - from: "gsd-runner/src/config.ts"
      to: "gsd-runner/src/types.ts"
      via: "imports RunnerConfig"
      pattern: "import.*RunnerConfig.*from.*types"
---

<objective>
Implement the session runner that wraps Agent SDK query() with bounded sessions, in-stream compaction tracking, and graceful abort support. Also implement config loading from environment.

Purpose: The session runner is the execution engine -- it translates GSD actions from the state machine into actual Claude Code sessions. Every safety mechanism (maxTurns, compaction detection, abort, auto-approve) is wired here. This is the highest-complexity component and must be rock-solid before the daemon loop can use it.

Output: session-runner.ts with runGsdCommand(), config.ts with loadConfig(), and passing mocked tests.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-engine/01-RESEARCH.md
@.planning/phases/01-core-engine/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -- types the executor needs -->

From gsd-runner/src/types.ts:
```typescript
export type GsdAction =
  | { type: 'plan'; phase: number }
  | { type: 'execute'; phase: number }
  | { type: 'verify'; phase: number }
  | { type: 'resume' }
  | { type: 'done' }
  | { type: 'error'; reason: string };

export interface ParsedState { ... }
export interface PhaseInfo { ... }

export interface RunnerConfig {
  projectDir: string;
  maxTurns: number;
  maxBudgetUsd: number;
  compactionThreshold: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config loader and session runner with mocked tests</name>
  <files>
    gsd-runner/src/config.ts
    gsd-runner/src/session-runner.ts
    gsd-runner/test/session-runner.test.ts
  </files>
  <behavior>
    - loadConfig reads from process.env and returns RunnerConfig with defaults (maxTurns=75, maxBudgetUsd=5, compactionThreshold=2)
    - loadConfig throws if PROJECT_DIR is not set
    - runGsdCommand calls query() with correct options: maxTurns from config, permissionMode 'bypassPermissions', allowDangerouslySkipPermissions true, settingSources ['project'], systemPrompt preset 'claude_code'
    - runGsdCommand returns SessionResult with success, sessionId, compactionCount, costUsd
    - runGsdCommand counts compact_boundary messages and sets thresholdHit=true when count >= config.compactionThreshold
    - runGsdCommand respects AbortController (passes it to query options)
    - runGsdCommand throws on missing result message (stream ended without SDKResultMessage)
  </behavior>
  <action>
    1. Create src/config.ts:
       - `loadConfig(): RunnerConfig` -- reads from process.env via dotenv
       - Required: PROJECT_DIR (throw if missing)
       - Defaults: MAX_TURNS=75, MAX_BUDGET_USD=5.0, COMPACTION_THRESHOLD=2
       - Parse numbers with parseInt/parseFloat, validate with zod

    2. Create src/session-runner.ts:
       - Export `SessionResult` interface:
         ```typescript
         export interface SessionResult {
           success: boolean;
           sessionId: string;
           compactionCount: number;
           thresholdHit: boolean;
           costUsd: number;
           resultSubtype: string;
         }
         ```
       - Export `runGsdCommand(prompt: string, config: RunnerConfig, controller: AbortController): Promise<SessionResult>`
       - Implementation follows research Pattern 3 (Complete Session Invocation):
         a. Call `query({ prompt, options: { cwd: config.projectDir, maxTurns: config.maxTurns, maxBudgetUsd: config.maxBudgetUsd, permissionMode: 'bypassPermissions', allowDangerouslySkipPermissions: true, systemPrompt: { type: 'preset', preset: 'claude_code' }, settingSources: ['project'], abortController: controller } })`
         b. Iterate async generator: track sessionId from init message, count compact_boundary messages, capture result message
         c. Return SessionResult with thresholdHit = compactionCount >= config.compactionThreshold
         d. Throw if stream ends without result message
       - Use pino logger for structured logging of session start, compaction events, session end

    3. Create test/session-runner.test.ts:
       - Mock `@anthropic-ai/claude-agent-sdk` module using vitest.mock()
       - Create helper that returns an async generator yielding configurable SDKMessage sequences
       - Tests:
         - "calls query with correct maxTurns from config" -- verify options passed to query()
         - "sets permissionMode to bypassPermissions" -- verify options
         - "sets settingSources to ['project']" -- verify options
         - "passes abortController to query options" -- verify options
         - "captures session ID from init message" -- yield init msg, check result.sessionId
         - "counts compaction events" -- yield 2 compact_boundary messages, check compactionCount=2
         - "sets thresholdHit when compaction count >= threshold" -- yield 3 compact_boundary with threshold=2, check thresholdHit=true
         - "thresholdHit false when below threshold" -- yield 1 compact_boundary with threshold=2, check thresholdHit=false
         - "returns success=true for successful result" -- yield result with subtype 'success'
         - "returns success=false for max_turns result" -- yield result with subtype 'error_max_turns'
         - "throws when stream ends without result" -- yield only init message, no result
         - "extracts cost from result message" -- yield result with total_cost_usd, check costUsd

    4. Run tests: `cd gsd-runner && npx vitest run --reporter=verbose` -- all pass including Plan 01 tests.
  </action>
  <verify>
    <automated>cd /home/agent/projects/get-shit-done/gsd-runner && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - src/config.ts exports loadConfig with env-based configuration and defaults
    - src/session-runner.ts exports runGsdCommand that wraps Agent SDK query() with all safety options
    - Compaction tracking works: counts compact_boundary messages and flags threshold
    - AbortController passed through for SIGTERM support
    - All tests pass (session-runner + state-parser + state-machine from Plan 01)
  </done>
</task>

</tasks>

<verification>
- `cd gsd-runner && npx vitest run --reporter=verbose` -- all tests pass
- `cd gsd-runner && npx tsc --noEmit` -- no TypeScript errors
- session-runner.ts imports from @anthropic-ai/claude-agent-sdk (key link verified)
- query() called with permissionMode: 'bypassPermissions' in every test
</verification>

<success_criteria>
- runGsdCommand correctly wraps Agent SDK with maxTurns, bypassPermissions, settingSources, abortController
- Compaction events tracked in-stream with configurable threshold
- Config loads from environment with sensible defaults
- All tests pass (new + prior plans)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine/01-02-SUMMARY.md`
</output>
