---
phase: 24
plan: 01
type: implementation
autonomous: true
wave: 1
requirements: [TE-11]
---

# Phase 24 Plan 01: GSD Execute-Plan Codex Integration

## Objective

Modify `upstream/gsd/commands/gsd/execute-plan.md` to route codex-safe tasks to Codex CLI and claude-required tasks to the Claude executor, with a config toggle (`codex.enabled`) to disable Codex routing entirely.

## Context

- @upstream/gsd/commands/gsd/execute-plan.md (current orchestrator to modify)
- @.planning/REQUIREMENTS.md (TE-11 definition)

## Tasks

<task id="1" type="auto">
<name>Add TE-11 requirement to REQUIREMENTS.md</name>
<description>
Define TE-11 in REQUIREMENTS.md under a new "v1.4 Requirements — Token Efficiency" section.
TE-11: `codex.enabled: false` in config.json bypasses Codex routing entirely.
Add traceability row mapping TE-11 to Phase 24.
</description>
<files>
- .planning/REQUIREMENTS.md
</files>
<done>
- TE-11 requirement defined with clear acceptance criteria
- Traceability table has TE-11 row pointing to Phase 24
</done>
</task>

<task id="2" type="auto">
<name>Add Step 4.5 and Step 5a to execute-plan.md, rename Step 5 to 5b</name>
<description>
Modify upstream/gsd/commands/gsd/execute-plan.md to insert Codex task routing:

**Step 4.5 (NEW) — Classify tasks for Codex routing:**
- Check `.planning/config.json` for `codex.enabled` (default: false)
- If codex.enabled is false or missing: skip to Step 5b (preserve existing behavior)
- If codex.enabled is true: parse plan tasks, run task-router.js to classify into codex_tasks[] and claude_tasks[]

**Step 5a (NEW) — Execute Codex tasks:**
- If codex_tasks is non-empty AND `codex` CLI is available: for each codex task, invoke codex-exec-wrapper.sh
- Collect results (commit hashes, pass/fail status)
- If codex.fallback_on_failure is true and a task fails: move it to claude_tasks[]

**Step 5b (EXISTING Step 5, renumbered):**
- Existing gsd-executor spawn logic, unchanged
- Add completed_tasks context from Step 5a results (if any)
- Renumber subsequent steps (6 becomes 6, 7 becomes 7 — no change needed since 5a/5b are sub-steps)
</description>
<files>
- upstream/gsd/commands/gsd/execute-plan.md
</files>
<done>
- Step 4.5 exists with codex.enabled check and task-router.js invocation
- Step 5a exists with codex-exec-wrapper.sh invocation loop
- Step 5b preserves existing Claude executor spawn with completed_tasks context
- codex.enabled: false bypasses Steps 4.5 and 5a entirely
</done>
</task>

<task id="3" type="auto">
<name>Create Codex config documentation</name>
<description>
Create a config documentation file describing the codex section of .planning/config.json:
- codex.enabled (bool, default false) — master toggle for Codex routing
- codex.timeout_seconds (int, default 300) — max time per Codex task
- codex.max_file_count (int, default 8) — tasks touching more files route to Claude
- codex.fallback_on_failure (bool, default true) — failed Codex tasks re-route to Claude

Place at docs/cortex/research/token-efficiency/codex-config-reference.md
</description>
<files>
- docs/cortex/research/token-efficiency/codex-config-reference.md
</files>
<done>
- Config reference doc exists with all four codex settings documented
- Each setting has type, default, and description
- Example config.json snippet included
</done>
</task>

## Verification

- [ ] execute-plan.md contains "Step 4.5" with task-router.js reference
- [ ] execute-plan.md contains "Step 5a" with codex-exec-wrapper.sh reference
- [ ] execute-plan.md contains "Step 5b" preserving existing gsd-executor spawn
- [ ] codex.enabled false/missing check gates 4.5 and 5a
- [ ] Config documentation exists at docs/cortex/research/token-efficiency/codex-config-reference.md
- [ ] TE-11 requirement defined in REQUIREMENTS.md

## Success Criteria

All six verification items pass. Existing execute-plan.md behavior is preserved when codex.enabled is false or absent.
