---
phase: 15-bridge-and-gsd-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/cortex-bridge/SKILL.md
  - test/bridge-output.test.sh
autonomous: true
requirements: [AUTON-05, AUTON-06]
must_haves:
  truths:
    - "/cortex-bridge reads gsd-handoff.md, spec.md, and contract and generates 6 GSD artifacts"
    - "Bridge-generated ROADMAP.md success criteria match contract done_criteria exactly"
    - "Bridge-generated artifacts are valid GSD format (PROJECT.md, ROADMAP.md, REQUIREMENTS.md, STATE.md, config.json, CONTEXT.md)"
  artifacts:
    - path: "skills/cortex-bridge/SKILL.md"
      provides: "Bridge command skill definition"
      contains: "cortex-bridge"
    - path: "test/bridge-output.test.sh"
      provides: "Bridge output validation tests"
      contains: "assert_pass"
  key_links:
    - from: "skills/cortex-bridge/SKILL.md"
      to: "docs/cortex/specs/{slug}/gsd-handoff.md"
      via: "reads gsd-handoff phases and requirement mapping"
      pattern: "gsd-handoff.md"
    - from: "skills/cortex-bridge/SKILL.md"
      to: "docs/cortex/contracts/{slug}/contract-001.md"
      via: "reads done_criteria for ROADMAP success criteria"
      pattern: "done_criteria"
    - from: "skills/cortex-bridge/SKILL.md"
      to: ".planning/"
      via: "writes 6 GSD artifacts"
      pattern: ".planning/"
---

<objective>
Build the `/cortex-bridge` command (SKILL.md) that generates a complete GSD milestone scaffold from Cortex artifacts, and a test script that validates bridge output correctness.

Purpose: Eliminates the manual handoff gap between Cortex intelligence output and GSD execution input. A single command generates all 6 GSD artifacts from existing Cortex specs, contracts, and handoff packs.
Output: skills/cortex-bridge/SKILL.md, test/bridge-output.test.sh
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-bridge-and-gsd-integration/15-CONTEXT.md
@docs/cortex/contracts/adaptive-autonomy/contract-001.md
@docs/cortex/specs/adaptive-autonomy/spec.md
@docs/cortex/specs/adaptive-autonomy/gsd-handoff.md
@docs/cortex/research/adaptive-autonomy/implementation-20260402T063000Z.md
@scripts/cortex/resolve-autonomy.js

<interfaces>
<!-- Existing resolver API (bridge will reference for autonomy config reads) -->
From scripts/cortex/resolve-autonomy.js:
```javascript
function resolveAutonomy(options) → { preset, gates }
// options: { invocationFlags?, projectConfig?, globalConfig? }
module.exports = { resolveAutonomy, PRESET_DEFAULTS, MANDATORY_GATES };
```

<!-- GSD artifact formats the bridge must generate (extracted from .planning/) -->

PROJECT.md structure:
- ## What This Is
- ## Core Value
- ## Requirements (### Validated, ### Active, ### Out of Scope)
- ## Context
- ## Constraints
- ## Key Decisions

ROADMAP.md structure:
- ## Overview
- ## Phases (### Phase N: Name)
  - **Goal**: ...
  - **Depends on**: ...
  - **Requirements**: REQ-01, REQ-02, ...
  - **Success Criteria** (numbered list)
  - **Plans**: N plans
- ## Progress (table)

REQUIREMENTS.md structure:
- ## {Category} Requirements
- - [ ] **REQ-ID**: description
- ## Traceability (table: Requirement | Phase | Status)

STATE.md structure:
- YAML frontmatter: gsd_state_version, milestone, milestone_name, status, stopped_at, last_updated, last_activity, progress
- ## Current Position
- ## Performance Metrics
- ## Accumulated Context (### Decisions, ### Pending Todos, ### Blockers/Concerns)
- ## Session Continuity

config.json structure:
```json
{
  "mode": "yolo",
  "granularity": "standard",
  "parallelization": true,
  "commit_docs": true,
  "model_profile": "balanced",
  "workflow": {
    "research": true,
    "plan_check": true,
    "verifier": true,
    "nyquist_validation": true,
    "auto_advance": true,
    "_auto_chain_active": true
  }
}
```

CONTEXT.md structure (per phase):
- <domain> ## Phase Boundary </domain>
- <decisions> ## Implementation Decisions </decisions>
- <specifics> ## Specific Ideas </specifics>
- <deferred> ## Deferred Ideas </deferred>

<!-- Test pattern from existing test files -->
From test/autonomy-config.test.sh:
```bash
#!/usr/bin/env bash
set -euo pipefail
REPO_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PASS=0; FAIL=0
assert_pass() { local name="$1"; PASS=$((PASS + 1)); printf "  PASS  %s\n" "$name"; }
assert_fail() { local name="$1"; local msg="$2"; FAIL=$((FAIL + 1)); printf "  FAIL  %s — %s\n" "$name" "$msg"; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /cortex-bridge SKILL.md</name>
  <files>skills/cortex-bridge/SKILL.md</files>
  <read_first>
    - docs/cortex/specs/adaptive-autonomy/spec.md (bridge architecture, Section 3-4)
    - docs/cortex/specs/adaptive-autonomy/gsd-handoff.md (input format bridge reads)
    - docs/cortex/contracts/adaptive-autonomy/contract-001.md (done_criteria that become ROADMAP success criteria per AUTON-06)
    - docs/cortex/research/adaptive-autonomy/implementation-20260402T063000Z.md (artifact mapping details)
    - .planning/PROJECT.md (output format — bridge must generate this structure)
    - .planning/ROADMAP.md (output format — bridge must generate this structure)
    - .planning/REQUIREMENTS.md (output format — bridge must generate this structure)
    - .planning/STATE.md (output format — bridge must generate this structure)
    - .planning/config.json (output format — bridge must generate this structure)
    - .planning/phases/15-bridge-and-gsd-integration/15-CONTEXT.md (output format for phase CONTEXT.md)
    - skills/cortex-clarify/SKILL.md (reference for skill file conventions: ## User-invocable, ## Arguments, ## Instructions pattern)
    - scripts/cortex/resolve-autonomy.js (resolver API the bridge reads for autonomy config)
  </read_first>
  <action>
Create `skills/cortex-bridge/SKILL.md` following the established Cortex skill pattern (see cortex-clarify/SKILL.md for format conventions). The skill must:

**Header section:**
- Title: `# Cortex Bridge — GSD Milestone Scaffold Generator`
- Description: Generates a complete GSD `.planning/` scaffold from Cortex artifacts (spec, contract, gsd-handoff). Eliminates manual Cortex-to-GSD handoff.
- User-invocable: `/cortex-bridge` trigger phrases: "bridge to GSD", "generate GSD scaffold", "import to GSD"
- Arguments:
  - `--slug <slug>` — explicit slug (optional; default: read from `.cortex/state.json` field `slug`)
  - `--autonomy <preset>` — override autonomy preset for this invocation
  - `--gate <name>=<bool>` — override specific gate
  - `--dry-run` — print what would be generated without writing files

**Phase 1: Resolve inputs.**
1. Determine slug: use `--slug` arg, else read `.cortex/state.json` → `slug` field. If neither exists, error: "No active slug. Run /cortex-clarify first or pass --slug."
2. Verify these Cortex artifacts exist (error with specific message if any missing):
   - `docs/cortex/specs/{slug}/gsd-handoff.md`
   - `docs/cortex/specs/{slug}/spec.md`
   - `docs/cortex/contracts/{slug}/contract-001.md`
3. Also attempt to read (optional, enhance output if present):
   - `docs/cortex/clarify/{slug}/` — latest clarify brief
   - `docs/cortex/research/{slug}/` — latest research dossiers

**Phase 2: Parse Cortex artifacts.**
Extract from each source:
- **gsd-handoff.md**: `## Objective`, `## Deliverables`, `## Requirements` (list of AUTON-XX IDs and descriptions), `## Tasks` (checklist), `## Acceptance Criteria`
- **spec.md**: `## 1. Problem`, `## 2. Scope` (In Scope / Out of Scope), `## 4. Interfaces`, `## 5. Dependencies`, `## 6. Risks`, `## 7. Sequencing`, `## 8. Tasks`
- **contract-001.md**: `## Done Criteria` (the checklist — each item becomes a ROADMAP success criterion per AUTON-06), `## Deliverables`, `## Scope`, `## Write Roots`, `## Validators`
- **clarify brief** (if exists): Goal, Non-goals, Constraints, Assumptions, Open Questions
- **research dossiers** (if exist): Summary, Findings, Recommendations, Trade-offs

**Phase 3: Generate GSD artifacts.**
Write each file to `.planning/`. If `.planning/` directory does not exist, create it. If files already exist, warn and ask for confirmation before overwriting (unless `--dry-run`).

**3a. PROJECT.md** — Generate from spec + clarify brief + contract:
```markdown
# {Milestone Name}

## What This Is

{From spec Section 1 Problem — first paragraph, rewritten as project description}

## Core Value

{From spec Section 1 Problem — the key outcome statement}

## Requirements

### Active

{For each requirement ID from gsd-handoff Requirements section:}
- [ ] {REQ-ID}: {description}

### Out of Scope

{From spec Section 2 Out of Scope items}

## Context

{From clarify brief if exists: Current baseline, Target, Ownership contract}
{If no clarify brief: "See contract and spec for full context."}

## Constraints

{From spec Section 2 In Scope constraints + contract Scope constraints}

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
{From research Trade-offs selected options, if research exists}
```

**3b. ROADMAP.md** — Generate from gsd-handoff + contract (AUTON-06 critical):
```markdown
# Roadmap: {Milestone Name}

## Overview

{From gsd-handoff Objective}

## Phases

{For each phase derived from gsd-handoff Tasks grouping or spec Sequencing:}

### Phase {N}: {Phase Name}
**Goal**: {Phase goal derived from task group}
**Depends on**: {Previous phase or "Nothing"}
**Requirements**: {Comma-separated REQ-IDs addressed by this phase}
**Success Criteria** (what must be TRUE):
{For EACH item in contract Done Criteria that maps to this phase:}
  {N}. {Exact text of the done_criteria item — per AUTON-06, these MUST match exactly}
**Research**: {Likely/Unlikely based on whether phase involves new integrations}
**Plans**: 0 plans

## Progress

| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
{For each phase: 0/0, Not started, -}
```

CRITICAL for AUTON-06: Each done_criteria line from the contract MUST appear verbatim as a success criterion in the corresponding ROADMAP phase. Do NOT paraphrase, summarize, or rewrite. Copy the exact text.

**3c. REQUIREMENTS.md** — Generate from contract done_criteria + gsd-handoff requirements:
```markdown
# Requirements: {Milestone Name}

**Defined:** {today's date}
**Core Value:** {From PROJECT.md Core Value}

## {Category} Requirements

{For each requirement from gsd-handoff Requirements:}
- [ ] **{REQ-ID}**: {description}

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
{For each REQ-ID: REQ-ID | Phase N: Name | Pending}

**Coverage:**
- {Category} requirements: {N} total -- all mapped
- Unmapped: 0
```

**3d. STATE.md** — Generate initial state:
```markdown
---
gsd_state_version: 1.0
milestone: {version from contract or "v1.0"}
milestone_name: {slug}
status: planning
stopped_at: Bridge import complete
last_updated: "{ISO timestamp}"
last_activity: {today YYYY-MM-DD}
progress:
  total_phases: {N}
  completed_phases: 0
  total_plans: 0
  completed_plans: 0
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated {today})

**Core value:** {From PROJECT.md}
**Current focus:** Phase 1 — {first phase name}

## Current Position

Phase: 1
Plan: Not started
Status: Ready for planning
Last activity: {today}

Progress: [{empty bar}] 0/0 plans; 0/{N} phases complete

## Accumulated Context

### Decisions

Bridge import from Cortex contract: {contract path}

### Pending Todos

None.

### Blockers/Concerns

None.

## Session Continuity

Last session: {ISO timestamp}
Stopped at: Bridge import complete
Resume file: None
```

**3e. config.json** — Generate GSD config with autonomy-derived flags:
```json
{
  "mode": "yolo",
  "granularity": "standard",
  "parallelization": true,
  "commit_docs": true,
  "model_profile": "balanced",
  "workflow": {
    "research": true,
    "plan_check": true,
    "verifier": true,
    "nyquist_validation": true,
    "auto_advance": true
  }
}
```

Additionally, sync autonomy flags into config.json (per CONTEXT.md decision "Config Sync"):
- Read resolved autonomy config via the resolver pattern
- If `gates.discuss_phase` is false, set `workflow.skip_discuss_cortex: true` in config.json
- This keeps the Cortex-to-GSD dependency direction clean (GSD reads config.json, not autonomy.json)

**3f. Phase CONTEXT.md files** — For each phase in the generated ROADMAP:
Write `{padded_phase}-CONTEXT.md` into the phase directory `.planning/phases/{NN}-{slug}/`.
Create the phase directory if it does not exist.

When Cortex clarify brief and research dossiers exist:
```markdown
# Phase {N}: {Name} - Context

**Gathered:** {date}
**Status:** Ready for planning
**Source:** Auto-populated from Cortex artifacts via /cortex-bridge

<domain>
## Phase Boundary

{Phase goal from ROADMAP}

</domain>

<decisions>
## Implementation Decisions

{Extract relevant decisions from spec Trade-offs and research Recommendations that apply to this phase}

### Claude's Discretion
{Any areas not covered by existing Cortex decisions}

</decisions>

<canonical_refs>
## Canonical References

{List the Cortex artifact paths that inform this phase}

</canonical_refs>

<specifics>
## Specific Ideas

{Extract relevant specifics from spec/research that apply to this phase}

</specifics>

<deferred>
## Deferred Ideas

{From spec Out of Scope items}

</deferred>

---

*Phase: {padded_phase}-{slug}*
*Context gathered: {date} via /cortex-bridge*
```

When clarify/research do NOT exist, generate minimal CONTEXT.md with decisions marked "Claude's Discretion" (same as current drive-workflow discuss behavior).

**Phase 4: Commit.**
After writing all artifacts:
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs({slug}): generate GSD scaffold via /cortex-bridge" --files .planning/PROJECT.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/config.json .planning/phases/*/
```

**Phase 5: Summary output.**
Print a summary table:
```
=== /cortex-bridge complete ===

Slug: {slug}
Source: {contract path}

Generated:
  .planning/PROJECT.md     — {line count} lines
  .planning/ROADMAP.md     — {line count} lines, {N} phases
  .planning/REQUIREMENTS.md — {N} requirements
  .planning/STATE.md       — initial state
  .planning/config.json    — workflow config
  Phase CONTEXT.md files   — {N} phases

Next: Run /gsd:drive or /gsd:plan-phase 1
```

**--dry-run behavior:** When `--dry-run` flag is present, print the summary table showing what WOULD be generated (artifact paths, line counts, phase count) but do NOT write any files, do NOT create directories, do NOT commit. Print "[DRY RUN] No files written." at the end.
  </action>
  <verify>
    <automated>grep -c "cortex-bridge" skills/cortex-bridge/SKILL.md && grep -c "gsd-handoff.md" skills/cortex-bridge/SKILL.md && grep -c "done_criteria" skills/cortex-bridge/SKILL.md && grep -c "ROADMAP.md" skills/cortex-bridge/SKILL.md && grep -c "dry-run" skills/cortex-bridge/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "# Cortex Bridge" skills/cortex-bridge/SKILL.md returns match
    - grep "gsd-handoff.md" skills/cortex-bridge/SKILL.md returns match (reads handoff)
    - grep "contract-001.md" skills/cortex-bridge/SKILL.md returns match (reads contract)
    - grep "done_criteria" skills/cortex-bridge/SKILL.md returns match (maps to success criteria per AUTON-06)
    - grep "PROJECT.md" skills/cortex-bridge/SKILL.md returns match (generates PROJECT.md)
    - grep "ROADMAP.md" skills/cortex-bridge/SKILL.md returns match (generates ROADMAP.md)
    - grep "REQUIREMENTS.md" skills/cortex-bridge/SKILL.md returns match (generates REQUIREMENTS.md)
    - grep "STATE.md" skills/cortex-bridge/SKILL.md returns match (generates STATE.md)
    - grep "config.json" skills/cortex-bridge/SKILL.md returns match (generates config.json)
    - grep "CONTEXT.md" skills/cortex-bridge/SKILL.md returns match (generates phase CONTEXT.md)
    - grep "dry-run" skills/cortex-bridge/SKILL.md returns match (supports --dry-run flag)
    - grep "autonomy" skills/cortex-bridge/SKILL.md returns match (reads autonomy config for flag sync)
    - grep "discuss_phase" skills/cortex-bridge/SKILL.md returns match (syncs discuss_phase gate to config.json)
  </acceptance_criteria>
  <done>SKILL.md exists at skills/cortex-bridge/SKILL.md with complete instructions for generating all 6 GSD artifacts from Cortex sources, --dry-run support, and autonomy flag sync to config.json</done>
</task>

<task type="auto">
  <name>Task 2: Create bridge output validation test script</name>
  <files>test/bridge-output.test.sh</files>
  <read_first>
    - test/autonomy-config.test.sh (test harness pattern — assert_pass/assert_fail, PASS/FAIL counters, set -euo pipefail)
    - skills/cortex-bridge/SKILL.md (the skill just created — understand what bridge generates)
    - docs/cortex/contracts/adaptive-autonomy/contract-001.md (done_criteria to verify AUTON-06 mapping)
    - docs/cortex/specs/adaptive-autonomy/gsd-handoff.md (requirements list bridge should extract)
  </read_first>
  <action>
Create `test/bridge-output.test.sh` following the established test pattern from `test/autonomy-config.test.sh` (bash, set -euo pipefail, assert_pass/assert_fail helpers, PASS/FAIL counters, summary at end).

The test script validates bridge output by creating a mock Cortex artifact set in a temp directory, then checking that the generated GSD artifacts have the correct structure and content.

**Test setup:**
```bash
#!/usr/bin/env bash
set -euo pipefail

REPO_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PASS=0
FAIL=0
TMPDIR=$(mktemp -d)
trap 'rm -rf "$TMPDIR"' EXIT

assert_pass() { local name="$1"; PASS=$((PASS + 1)); printf "  PASS  %s\n" "$name"; }
assert_fail() { local name="$1"; local msg="$2"; FAIL=$((FAIL + 1)); printf "  FAIL  %s — %s\n" "$name" "$msg"; }
```

**Create mock Cortex artifacts in $TMPDIR:**

1. `$TMPDIR/docs/cortex/specs/test-slug/gsd-handoff.md` with:
   - `## Objective` containing "Test objective for bridge validation"
   - `## Requirements` containing "- TEST-01: First requirement" and "- TEST-02: Second requirement"
   - `## Tasks` containing 3 task items

2. `$TMPDIR/docs/cortex/specs/test-slug/spec.md` with:
   - `## 1. Problem` containing "Test problem description"
   - `## 2. Scope` with In Scope and Out of Scope subsections

3. `$TMPDIR/docs/cortex/contracts/test-slug/contract-001.md` with:
   - `## Done Criteria` containing:
     ```
     - [ ] TEST-01: First test requirement is met exactly as specified
     - [ ] TEST-02: Second test requirement produces correct output
     ```
   - `## Deliverables` containing 2 items
   - `## Validators` containing 2 items

4. `$TMPDIR/.cortex/state.json` with `{"slug": "test-slug", "mode": "execute"}`

**Test cases (8 assertions):**

1. **"PROJECT.md has required sections"** — Check generated PROJECT.md contains:
   - `## What This Is`
   - `## Core Value`
   - `## Requirements`
   - `## Out of Scope` or `### Out of Scope`

2. **"ROADMAP.md has phase structure"** — Check generated ROADMAP.md contains:
   - `## Overview`
   - `### Phase`
   - `**Goal**:`
   - `**Requirements**:`
   - `**Success Criteria**`

3. **"ROADMAP.md success criteria match contract done_criteria (AUTON-06)"** — This is the critical test. Check that:
   - Generated ROADMAP.md contains the EXACT string "First test requirement is met exactly as specified"
   - Generated ROADMAP.md contains the EXACT string "Second test requirement produces correct output"
   - These are verbatim from the mock contract's Done Criteria (not paraphrased)

4. **"REQUIREMENTS.md has requirement IDs"** — Check generated REQUIREMENTS.md contains:
   - `**TEST-01**`
   - `**TEST-02**`
   - `## Traceability`

5. **"STATE.md has valid frontmatter"** — Check generated STATE.md contains:
   - `gsd_state_version: 1.0`
   - `milestone_name: test-slug`
   - `status: planning`

6. **"config.json is valid JSON"** — Check generated config.json:
   - `node -e "JSON.parse(require('fs').readFileSync('$TMPDIR/.planning/config.json','utf8'))"` exits 0
   - Contains `"workflow"` key

7. **"config.json syncs autonomy discuss_phase flag"** — When `.cortex/autonomy.json` has `{"preset": "full-auto"}`, check config.json contains `skip_discuss_cortex` (since full-auto disables discuss_phase gate).

8. **"All 6 artifacts exist"** — Check file existence:
   - `$TMPDIR/.planning/PROJECT.md`
   - `$TMPDIR/.planning/ROADMAP.md`
   - `$TMPDIR/.planning/REQUIREMENTS.md`
   - `$TMPDIR/.planning/STATE.md`
   - `$TMPDIR/.planning/config.json`

**Summary output:**
```bash
echo ""
echo "bridge-output: $PASS passed, $FAIL failed"
if [[ $FAIL -gt 0 ]]; then exit 1; fi
```

NOTE: Since the bridge is a SKILL.md (Claude instruction file, not executable code), the test script cannot invoke the bridge directly. Instead, the test validates the SKILL.md content structure to ensure it contains the necessary instructions. The test checks:
- SKILL.md references all 6 output artifacts by name
- SKILL.md contains the exact strings "done_criteria" and "verbatim" (or "exact") to ensure AUTON-06 compliance instructions
- SKILL.md contains instructions for all required input artifact paths
- SKILL.md contains --dry-run handling instructions

For the structural content tests (1-8 above), write them as validation functions that can be run against ACTUAL bridge output after a human runs `/cortex-bridge`. The test creates the mock artifacts, then checks whether the expected patterns would be satisfied. Structure tests as:
```bash
# Test SKILL.md contains required output instructions
if grep -q "PROJECT.md" "$REPO_DIR/skills/cortex-bridge/SKILL.md" && \
   grep -q "ROADMAP.md" "$REPO_DIR/skills/cortex-bridge/SKILL.md" && \
   grep -q "REQUIREMENTS.md" "$REPO_DIR/skills/cortex-bridge/SKILL.md" && \
   grep -q "STATE.md" "$REPO_DIR/skills/cortex-bridge/SKILL.md" && \
   grep -q "config.json" "$REPO_DIR/skills/cortex-bridge/SKILL.md"; then
  assert_pass "SKILL.md references all 6 output artifacts"
else
  assert_fail "SKILL.md references all 6 output artifacts" "missing artifact reference"
fi
```

Write at least 8 test assertions covering: SKILL.md completeness, output artifact references, AUTON-06 verbatim instruction, input artifact references, dry-run instruction, autonomy flag sync instruction, slug discovery instruction, commit instruction.
  </action>
  <verify>
    <automated>bash test/bridge-output.test.sh</automated>
  </verify>
  <acceptance_criteria>
    - bash test/bridge-output.test.sh exits 0
    - Output contains "PASS" for at least 8 assertions
    - Output contains "0 failed"
    - grep "AUTON-06" test/bridge-output.test.sh returns match (tests done_criteria mapping)
    - grep "assert_pass\|assert_fail" test/bridge-output.test.sh returns at least 8 matches
  </acceptance_criteria>
  <done>Test script passes all assertions, validates SKILL.md contains instructions for all 6 artifacts, AUTON-06 verbatim criteria mapping, dry-run support, and autonomy flag sync</done>
</task>

</tasks>

<verification>
- skills/cortex-bridge/SKILL.md exists and contains complete bridge instructions
- test/bridge-output.test.sh passes with 0 failures
- Bridge SKILL.md covers all 6 GSD output artifacts (PROJECT.md, ROADMAP.md, REQUIREMENTS.md, STATE.md, config.json, CONTEXT.md)
- Bridge SKILL.md explicitly instructs verbatim copying of contract done_criteria to ROADMAP success criteria (AUTON-06)
- Bridge SKILL.md includes --dry-run mode and autonomy flag sync to config.json
</verification>

<success_criteria>
- /cortex-bridge SKILL.md is complete and follows Cortex skill conventions
- Bridge reads gsd-handoff.md, spec.md, and contract as inputs
- Bridge generates all 6 GSD artifacts in .planning/
- AUTON-06: done_criteria -> success criteria mapping is explicit and verbatim
- Test script validates SKILL.md structure and passes all assertions
- AUTON-05 and AUTON-06 are fully addressed
</success_criteria>

<output>
After completion, create `.planning/phases/15-bridge-and-gsd-integration/15-01-SUMMARY.md`
</output>
