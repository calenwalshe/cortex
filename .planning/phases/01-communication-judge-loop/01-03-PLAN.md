---
plan: 01-03
phase: 01-communication-judge-loop
wave: 2
type: standard
depends: ["01-01"]
objective: Wire judge_communication() into cortex-drive SKILL.md Phase 6 with retry loop and escalation block
must_haves:
  truths:
    - SKILL.md Phase 6 calls judge_communication() after generating the Level 1 summary
    - retry loop in SKILL.md: pass→deliver, fail+attempts<3→rewrite+retry, cap→escalate to owner
    - escalation path presents original + final rewrite + critique (does NOT silently deliver any version)
    - judge does NOT gate Level 2+ summaries or internal messages
  key_links:
    - /home/agent/.claude/skills/cortex-drive/SKILL.md (modify Phase 6)
    - docs/cortex/rubrics/communication-judge-loop/drive-summary.yaml (read-only, created in 01-02)
---

# Plan 01-03: cortex-drive Phase 6 Integration

## Objective

Wire `judge_communication()` into `cortex-drive/SKILL.md` Phase 6 so that every drive completion summary is evaluated before delivery. If FAIL: structured critique-guided rewrite, retry up to 3 attempts. If retry cap exhausted: escalate to owner with original + final rewrite + critique.

## Execution Context

- File to modify: `/home/agent/.claude/skills/cortex-drive/SKILL.md`
- Section to modify: `### Phase 6: Completion Summary` (lines ~113-151)
- Integration point: after Level 1 summary is generated, before delivery
- Rubric path: `docs/cortex/rubrics/communication-judge-loop/drive-summary.yaml`
- Scope: Level 1 (owner) summaries ONLY. Level 2+ and internal messages are not gated.

## Context

### Current Phase 6 flow (from SKILL.md lines 113-151)

```
Read docs/cortex/display.json for report_level (default: 1)
Generate Level 1 summary using the 3-part formula (status line, delta bullets, risk line)
[deliver summary]
```

### New Phase 6 flow (with judge gate)

```
Read docs/cortex/display.json for report_level (default: 1)
Generate Level 1 summary using the 3-part formula

[JUDGE GATE — Level 1 only]
Call judge_communication(summary, rubric_path='docs/cortex/rubrics/communication-judge-loop/drive-summary.yaml')

If verdict == PASS:
  Deliver summary as-is.

If verdict == FAIL and attempts < 3:
  Rewrite summary guided by critique.
  Increment attempt counter.
  Re-evaluate. Repeat.

If verdict == FAIL and attempts == 3:
  ESCALATE to owner:
    Present: original summary + final rewrite attempt + critique
    Do NOT deliver any version silently.
    Next step: owner decides which version (if any) to accept.
```

### Integration note

The SKILL.md is an instruction document read by Claude (not executed code). The integration must be prose instructions that Claude can follow — not Python code. Pseudocode with clear conditional logic is appropriate.

## Tasks

- [ ] **T1**: Read `/home/agent/.claude/skills/cortex-drive/SKILL.md` Phase 6 section in full
- [ ] **T2**: Insert judge gate block immediately after Level 1 summary generation (before delivery):
  ```
  **Judge Gate (Level 1 summaries only):**

  After generating the Level 1 summary, evaluate it before delivery:

  1. Call `judge_communication(summary, rubric_path='docs/cortex/rubrics/communication-judge-loop/drive-summary.yaml')`.
  2. If `verdict == PASS`: deliver summary as-is. Proceed to Decision Logging.
  3. If `verdict == FAIL` and attempts < 3:
     - Log the critique to `docs/cortex/handoffs/decisions.md`.
     - Rewrite the summary guided by the critique findings. Preserve all uncertainty markers and caveats from the original — do not add false confidence.
     - Increment attempt counter. Return to step 1.
  4. If `verdict == FAIL` and attempts == 3 (retry cap exhausted):
     - **ESCALATE** — do NOT silently deliver any version.
     - Present to owner:
       ```
       ## Summary Quality Escalation — {slug}

       The communication judge could not produce a passing summary after 3 attempts.

       **Original summary:**
       {original_summary}

       **Final rewrite attempt:**
       {final_rewrite}

       **Critique (why it failed):**
       {critique}

       Owner action required: accept one of the above, request another rewrite, or proceed without a summary.
       ```

  Note: This gate applies to Level 1 (owner) summaries only. Level 2+ summaries and internal drive log messages are not evaluated.
  ```
- [ ] **T3**: Confirm Level 2+ path is explicitly excluded (add parenthetical or note if not already clear)
- [ ] **T4**: Verify the SKILL.md edit does not break existing Phase 6 structure (Level 2+ template still present, decision logging section intact)

## Verification

```bash
# Confirm judge gate text present in SKILL.md
grep -n "judge_communication\|Judge Gate\|ESCALATE" /home/agent/.claude/skills/cortex-drive/SKILL.md
```

Output should show the judge gate block in Phase 6.

```bash
# Confirm drive-summary rubric exists (dependency from 01-02)
test -f docs/cortex/rubrics/communication-judge-loop/drive-summary.yaml && echo "OK"
```

## Success Criteria

1. `SKILL.md` Phase 6 contains `judge_communication()` call with rubric path
2. SKILL.md contains retry loop: PASS→deliver, FAIL+attempts<3→rewrite, cap→escalate
3. Escalation block presents original + final rewrite + critique (not silent delivery)
4. Note explicitly excludes Level 2+ summaries and internal messages
5. Existing Phase 6 structure (Level 2 template, decision logging) is intact

## Output

- Modified: `/home/agent/.claude/skills/cortex-drive/SKILL.md` (Phase 6 section)
