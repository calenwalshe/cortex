---
phase: 02-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/cortex/clarify-brief.md
  - templates/cortex/current-understanding.md
autonomous: true
must_haves:
  truths:
    - "clarify-brief.md template documents initial_terminal_set: and ruled_out: fields with worked example"
    - "current-understanding.md template exists with all five required sections"
    - "Seven terminal slugs are enumerated in at least one of the two templates"
  artifacts:
    - path: "templates/cortex/clarify-brief.md"
      provides: "Updated clarify brief template with terminal-set frontmatter"
      contains: "initial_terminal_set"
    - path: "templates/cortex/current-understanding.md"
      provides: "New current-understanding template with Possible Terminals, Durable Findings, Provisional Thoughts, Open Questions, Iteration History"
      min_lines: 40
  key_links: []
---

<objective>
Add terminal-state frontmatter documentation to the clarify brief template and create the new current-understanding.md template.

Purpose: Provide the artifact foundation for the seven-terminal model before skill modifications.
Output: Updated templates/cortex/clarify-brief.md + new templates/cortex/current-understanding.md
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@templates/cortex/clarify-brief.md
@docs/cortex/clarify/clarify-research-loop/20260412T011953Z-clarify-brief.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add terminal-set frontmatter to clarify-brief.md template</name>
  <files>templates/cortex/clarify-brief.md</files>
  <action>
    Add a YAML frontmatter block at the top of the template (before the # Clarify Brief heading) with optional fields:
      - initial_terminal_set: (list, default = all six non-transitional terminals)
      - ruled_out: (list, default = [])
    Include inline comments explaining what each field means and that they are optional (backward compatible).
    Add a worked example block (as a comment) that shows exactly the iter-3 brief frontmatter from docs/cortex/clarify/clarify-research-loop/20260412T011953Z-clarify-brief.md as the canonical pattern.
    The six non-transitional terminals to enumerate: commit-to-build, kill-with-learning, decompose, experiment-required, already-exists, hold-on-dependency.
    Note: reframe-and-continue is the seventh terminal but is transitional (loops back into clarify), so it does NOT appear in initial_terminal_set — it is reached implicitly when a new clarify iteration supersedes the prior brief.
  </action>
  <verify>grep "initial_terminal_set" templates/cortex/clarify-brief.md returns a match</verify>
  <done>Template contains initial_terminal_set: and ruled_out: fields with inline docs and worked example. Existing non-frontmatter sections unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create current-understanding.md template</name>
  <files>templates/cortex/current-understanding.md</files>
  <action>
    Create a new file with YAML frontmatter (slug:, brief_iteration:, last_updated:) and five sections in this order:
    1. ## Possible Terminals — Markdown table: Terminal | Status | Ruled-Out Reason | Evidence. Six rows for the six non-transitional terminals. Status values: "live" or "ruled-out". Ruled-Out Reason empty for live terminals.
    2. ## Durable Findings — bullets, findings that have survived across iterations
    3. ## Provisional Thoughts — bullets marked explicitly as provisional, open spec-level details
    4. ## Open Questions — bullets with trigger conditions for revisit (from deferred-gaps pattern)
    5. ## Iteration History — Markdown table: Iteration | Brief | Dossier | Reframe Reason
    Include placeholder text ({SLUG}, {ITERATION}, {DATE}) for fields that cortex-clarify Phase 4b will populate.
    Add a header comment explaining this file is auto-generated by /cortex-clarify and updated each iteration.
  </action>
  <verify>wc -l templates/cortex/current-understanding.md returns ≥40</verify>
  <done>File exists with all five sections, YAML frontmatter, placeholder fields, and header comment. Six terminal rows in Possible Terminals table.</done>
</task>

</tasks>

<verification>
- [ ] grep "initial_terminal_set" templates/cortex/clarify-brief.md succeeds
- [ ] grep "Possible Terminals" templates/cortex/current-understanding.md succeeds
- [ ] grep "Iteration History" templates/cortex/current-understanding.md succeeds
- [ ] grep "commit-to-build" templates/cortex/current-understanding.md succeeds
</verification>

<success_criteria>
- Both template files exist with required content
- Seven terminal slugs documented in at least one template
- Backward compatibility: existing clarify briefs without frontmatter fields still valid
</success_criteria>

<output>
After completion, create .planning/phases/02-templates/02-01-SUMMARY.md
</output>
