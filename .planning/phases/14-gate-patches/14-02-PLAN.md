---
phase: 14-gate-patches
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/cortex-spec/SKILL.md
  - skills/cortex-review/SKILL.md
autonomous: true
requirements: [AUTON-01, AUTON-02]

must_haves:
  truths:
    - "With preset full-auto, cortex-spec critical_uncertainty and evidence_backing checks auto-proceed"
    - "With preset full-auto, cortex-spec contract_approval auto-proceeds"
    - "With preset full-auto, cortex-review eval_validation and compliance_verdict auto-proceed"
    - "With preset supervised (or no config), all gates in cortex-spec and cortex-review behave identically to pre-patch behavior"
    - "Mandatory gate reclarify in cortex-spec always blocks even in full-auto"
  artifacts:
    - path: "skills/cortex-spec/SKILL.md"
      provides: "Autonomy-aware reclarify (mandatory), critical_uncertainty, evidence_backing, contract_approval gates"
      contains: "resolve-autonomy"
    - path: "skills/cortex-review/SKILL.md"
      provides: "Autonomy-aware eval_validation and compliance_verdict gates"
      contains: "resolve-autonomy"
  key_links:
    - from: "skills/cortex-spec/SKILL.md"
      to: "scripts/cortex/resolve-autonomy.js"
      via: "autonomy config resolution instruction"
      pattern: "resolve-autonomy"
    - from: "skills/cortex-review/SKILL.md"
      to: "scripts/cortex/resolve-autonomy.js"
      via: "autonomy config resolution instruction"
      pattern: "resolve-autonomy"
---

<objective>
Patch cortex-spec and cortex-review SKILL.md files with conditional gate wrappers. cortex-spec has 4 gates (reclarify is mandatory, plus critical_uncertainty, evidence_backing, contract_approval). cortex-review has 2 gates (eval_validation, compliance_verdict).

Purpose: These two skills have the most complex gate logic in the pipeline. After this plan, preset: "full-auto" auto-proceeds past non-mandatory gates while the mandatory reclarify gate always blocks. Preset: "supervised" preserves existing behavior.

Output: Two patched SKILL.md files with autonomy-conditional gate instructions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-gate-patches/14-CONTEXT.md
@.planning/phases/13-autonomy-config-foundation/13-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From scripts/cortex/resolve-autonomy.js:
```javascript
const MANDATORY_GATES = ['ux_taste_eval', 'human_action', 'reclarify'];

// Resolution: invocation > project > global > preset defaults
// Mandatory gates forced true AFTER full merge
const { preset, gates } = resolveAutonomy({ projectConfig, globalConfig, invocationFlags });
// gates.reclarify → always true (mandatory)
// gates.critical_uncertainty → boolean (false in full-auto)
// gates.evidence_backing → boolean (false in full-auto)
// gates.contract_approval → boolean (false in full-auto, true in gates-only and supervised)
// gates.eval_validation → boolean (false in full-auto and gates-only)
// gates.compliance_verdict → boolean (false in full-auto and gates-only)
```

From templates/cortex/autonomy.json (gate names that map to skills in this plan):
```json
{
  "gates": {
    "reclarify": true,            // cortex-spec Phase 1 step 6 — MANDATORY
    "critical_uncertainty": true,  // cortex-spec Phase 1 step 7
    "evidence_backing": true,      // cortex-spec Phase 1 step 8
    "contract_approval": true,     // cortex-spec Phase 5 (approval_status)
    "eval_validation": true,       // cortex-review Contract Compliance (eval plan check)
    "compliance_verdict": true     // cortex-review Contract Compliance (verdict)
  }
}
```

Gate mapping to cortex-spec SKILL.md locations:
- reclarify: Phase 1, step 6 — "If reclarify_required: true, block with..." (MANDATORY — always enforced)
- critical_uncertainty: Phase 1, step 7 — "Check for entries where severity: critical AND status: open"
- evidence_backing: Phase 1, step 8 — "For each core assumption...verify it is backed by at least one research finding"
- contract_approval: Phase 5 — "approval_status: pending (spec and contract require human approval)"

Gate mapping to cortex-review SKILL.md locations:
- eval_validation: Contract Compliance section — "Eval Plan Validation" sub-block checking eval_plan field
- compliance_verdict: Contract Compliance section — "CONTRACT COMPLIANCE: COMPLIANT | NON-COMPLIANT" verdict
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch cortex-spec with 4 gate wrappers (reclarify mandatory + 3 conditional)</name>
  <files>skills/cortex-spec/SKILL.md</files>
  <read_first>
    - skills/cortex-spec/SKILL.md
    - scripts/cortex/resolve-autonomy.js
    - templates/cortex/autonomy.json
    - .planning/phases/14-gate-patches/14-CONTEXT.md
  </read_first>
  <action>
In `skills/cortex-spec/SKILL.md`, Phase 1 ("Validate Prerequisites") contains steps 6, 7, and 8 which are the gates. Phase 5 has the contract approval gate.

**Step A: Add autonomy resolution block at the start of Phase 1**

After the Phase 1 heading "### Phase 1: Validate Prerequisites" and before step 1 ("Read `.cortex/state.json`..."), insert:

```markdown
**Autonomy config resolution:**
Before evaluating prerequisite gates, resolve the autonomy config once for this invocation:
1. Read `.cortex/autonomy.json` (project-level) and `~/.claude/cortex-autonomy.json` (global-level) if they exist.
2. Determine the active preset (default: `supervised` if no config found).
3. Resolve all gate values using 4-layer precedence: invocation flags > project config > global config > preset defaults. Mandatory gates (`ux_taste_eval`, `human_action`, `reclarify`) are always forced true regardless of any config.
4. The resolved gate values are used in steps 6, 7, and 8 below, and in Phase 5.

The following gates apply to this skill:
- `reclarify` — **MANDATORY** (always true, cannot be disabled). Controls step 6.
- `critical_uncertainty` — Controls step 7. False in `full-auto` preset.
- `evidence_backing` — Controls step 8. False in `full-auto` preset.
- `contract_approval` — Controls Phase 5 approval gate. False in `full-auto` preset.
```

**Step B: Annotate step 6 (reclarify) as mandatory**

Step 6 currently reads:
```
6. Check `.cortex/state.json` for `reclarify_required`.
   - If `reclarify_required: true`, block with:
```

Change to:
```
6. **Gate: `reclarify` (MANDATORY — always enforced regardless of autonomy preset)**
   Check `.cortex/state.json` for `reclarify_required`.
   - If `reclarify_required: true`, block with:
```

The block message and behavior remain UNCHANGED. This gate cannot be skipped.

**Step C: Wrap step 7 (critical_uncertainty) with autonomy conditional**

Step 7 currently reads:
```
7. Read `docs/cortex/handoffs/open-questions.md`. Check for any entries where both `severity: critical` AND `status: open`.
   - If any such entries exist, block with:
```

Change to:
```
7. **Gate: `critical_uncertainty` (autonomy-conditional)**
   If `gates.critical_uncertainty` is `false` (per autonomy resolution above): skip this check — auto-proceed. Log the skip by appending to `docs/cortex/handoffs/decisions.md`: `| {ISO timestamp} | critical_uncertainty | auto-skipped | autonomy: {preset} |`.
   If `gates.critical_uncertainty` is `true` (or no autonomy config exists): evaluate as follows:
   Read `docs/cortex/handoffs/open-questions.md`. Check for any entries where both `severity: critical` AND `status: open`.
   - If any such entries exist, block with:
```

The block message and backward-compat note about flat/legacy entries remain UNCHANGED.

**Step D: Wrap step 8 (evidence_backing) with autonomy conditional**

Step 8 currently reads:
```
8. Inspect all research dossiers for the active slug...
```

Change to:
```
8. **Gate: `evidence_backing` (autonomy-conditional)**
   If `gates.evidence_backing` is `false` (per autonomy resolution above): skip this check — auto-proceed. Log the skip by appending to `docs/cortex/handoffs/decisions.md`: `| {ISO timestamp} | evidence_backing | auto-skipped | autonomy: {preset} |`.
   If `gates.evidence_backing` is `true` (or no autonomy config exists): evaluate as follows:
   Inspect all research dossiers for the active slug (`docs/cortex/research/{slug}/*.md`). For each core assumption listed in any dossier, verify it is backed by at least one research finding or experiment result within the dossiers.
```

The block message remains UNCHANGED.

**Step E: Wrap Phase 5 contract_approval gate**

In Phase 5 ("Update Continuity State"), the Rules section says: "The spec and contract require human approval before any execution begins. This is a hard gate."

Add after the Phase 5 state updates and before the Rules section:

```markdown
**Gate: `contract_approval` (autonomy-conditional)**
If `gates.contract_approval` is `false` (per autonomy resolution from Phase 1): auto-approve — set `approval_status` to `approved` instead of `pending` in both `current-state.md` and `.cortex/state.json`. Log the skip by appending to `docs/cortex/handoffs/decisions.md`: `| {ISO timestamp} | contract_approval | auto-skipped | autonomy: {preset} |`. Update `next_action` to skip the manual approval step.
If `gates.contract_approval` is `true` (or no autonomy config exists): set `approval_status` to `pending` as currently specified (existing behavior preserved).
```

Also update the Rules bullet about human approval. Change:
```
- **The spec and contract require human approval before any execution begins.** This is a hard gate — approval_status must be set to `approved` before GSD execution can start.
```
To:
```
- **The spec and contract require human approval before any execution begins — unless the `contract_approval` autonomy gate is disabled.** When the gate is active, approval_status must be set to `approved` manually before GSD execution can start. When the gate is disabled, the system auto-approves.
```
  </action>
  <verify>
    <automated>grep -c "autonomy" skills/cortex-spec/SKILL.md | grep -q "^[5-9]\|^[1-9][0-9]" && grep -c "auto-skipped" skills/cortex-spec/SKILL.md | grep -q "^[3-9]" && grep -q "MANDATORY" skills/cortex-spec/SKILL.md && grep -q "critical_uncertainty" skills/cortex-spec/SKILL.md && grep -q "evidence_backing" skills/cortex-spec/SKILL.md && grep -q "contract_approval" skills/cortex-spec/SKILL.md && grep -q "decisions.md" skills/cortex-spec/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Autonomy config resolution" skills/cortex-spec/SKILL.md returns a match
    - grep "MANDATORY" skills/cortex-spec/SKILL.md returns a match (reclarify gate annotated)
    - grep "critical_uncertainty" skills/cortex-spec/SKILL.md returns at least 2 matches
    - grep "evidence_backing" skills/cortex-spec/SKILL.md returns at least 2 matches
    - grep "contract_approval" skills/cortex-spec/SKILL.md returns at least 2 matches
    - grep "auto-skipped" skills/cortex-spec/SKILL.md returns at least 3 matches (one per non-mandatory gate)
    - grep "decisions.md" skills/cortex-spec/SKILL.md returns at least 3 matches
    - grep "reclarify_required: true" skills/cortex-spec/SKILL.md still returns a match (original block unchanged)
    - grep "BLOCKED:" skills/cortex-spec/SKILL.md returns at least 3 matches (original block messages preserved)
    - The Phase 2 (Synthesize Spec), Phase 3 (GSD Handoff), Phase 4 (Contract) sections are UNCHANGED
  </acceptance_criteria>
  <done>cortex-spec SKILL.md has 4 autonomy gates: reclarify (mandatory, always blocks), critical_uncertainty (conditional), evidence_backing (conditional), contract_approval (conditional). Full-auto skips 3 of 4. Supervised preserves all existing behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Patch cortex-review with eval_validation and compliance_verdict gate wrappers</name>
  <files>skills/cortex-review/SKILL.md</files>
  <read_first>
    - skills/cortex-review/SKILL.md
    - scripts/cortex/resolve-autonomy.js
    - templates/cortex/autonomy.json
    - .planning/phases/14-gate-patches/14-CONTEXT.md
  </read_first>
  <action>
In `skills/cortex-review/SKILL.md`, the Contract Compliance section contains two gates: eval plan validation (eval_validation) and the compliance verdict (compliance_verdict).

**Step A: Add autonomy resolution block at the start of the Contract Compliance section**

The "### Contract Compliance" heading currently says: "This section is required — it cannot be omitted."

After that line, insert:

```markdown
**Autonomy config resolution:**
Before evaluating contract compliance gates, resolve the autonomy config:
1. Read `.cortex/autonomy.json` (project-level) and `~/.claude/cortex-autonomy.json` (global-level) if they exist.
2. Determine the active preset (default: `supervised` if no config found).
3. Resolve all gate values using 4-layer precedence: invocation flags > project config > global config > preset defaults. Mandatory gates (`ux_taste_eval`, `human_action`, `reclarify`) are always forced true regardless of config.

The following gates apply to this section:
- `eval_validation` — Controls the eval plan validation block. False in `full-auto` and `gates-only` presets.
- `compliance_verdict` — Controls the compliance verdict. False in `full-auto` and `gates-only` presets.
```

**Step B: Wrap the Eval Plan Validation block with autonomy conditional**

The current "**Eval Plan Validation:**" block reads the eval_plan field and produces `[BLOCK]` issues.

Before the line "Read `eval_plan:` field from the active contract.", insert:

```markdown
**Gate: `eval_validation` (autonomy-conditional)**
If `gates.eval_validation` is `false` (per autonomy resolution above): skip the eval plan validation block — do not produce `[BLOCK]` issues for missing or pending eval plans. Instead, produce a `[NOTE]` level entry: `[NOTE] contract eval_plan — eval plan validation skipped (autonomy: {preset})`. Log the skip by appending to `docs/cortex/handoffs/decisions.md`: `| {ISO timestamp} | eval_validation | auto-skipped | autonomy: {preset} |`.
If `gates.eval_validation` is `true` (or no autonomy config exists): evaluate as follows:
```

The existing eval plan validation logic (checking for pending/TBD/empty, checking file existence) remains UNCHANGED under the "evaluate as follows" branch.

**Step C: Wrap the compliance verdict with autonomy conditional**

The current compliance verdict line reads:
```
State an overall compliance verdict:
CONTRACT COMPLIANCE: COMPLIANT | NON-COMPLIANT | PARTIALLY COMPLIANT
```

Change to:

```markdown
**Gate: `compliance_verdict` (autonomy-conditional)**
If `gates.compliance_verdict` is `false` (per autonomy resolution above): auto-proceed — still produce the compliance verdict line in the output (for logging), but do NOT block the pipeline on a NON-COMPLIANT verdict. Log the skip by appending to `docs/cortex/handoffs/decisions.md`: `| {ISO timestamp} | compliance_verdict | auto-skipped | autonomy: {preset} |`. The review artifact is written regardless.
If `gates.compliance_verdict` is `true` (or no autonomy config exists): state an overall compliance verdict as currently specified. A NON-COMPLIANT verdict blocks the pipeline (existing behavior preserved):
```

```
CONTRACT COMPLIANCE: COMPLIANT | NON-COMPLIANT | PARTIALLY COMPLIANT
```

**Step D: Leave all other sections unchanged**

Do NOT modify: Phase 0, sections 1-4 (Engineering/Security/YAGNI lenses), section 5 (Output Format), section 6 (Handling Pushback), Eval Failure Check, Store Results. These are review mechanics, not gates.
  </action>
  <verify>
    <automated>grep -c "autonomy" skills/cortex-review/SKILL.md | grep -q "^[3-9]\|^[1-9][0-9]" && grep -c "auto-skipped" skills/cortex-review/SKILL.md | grep -q "^[2-9]" && grep -q "eval_validation" skills/cortex-review/SKILL.md && grep -q "compliance_verdict" skills/cortex-review/SKILL.md && grep -q "decisions.md" skills/cortex-review/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Autonomy config resolution" skills/cortex-review/SKILL.md returns a match
    - grep "eval_validation" skills/cortex-review/SKILL.md returns at least 2 matches
    - grep "compliance_verdict" skills/cortex-review/SKILL.md returns at least 2 matches
    - grep "auto-skipped" skills/cortex-review/SKILL.md returns at least 2 matches
    - grep "decisions.md" skills/cortex-review/SKILL.md returns at least 2 matches
    - grep "BLOCK.*eval_plan" skills/cortex-review/SKILL.md still returns matches (original block messages preserved for gate-active path)
    - grep "CONTRACT COMPLIANCE:" skills/cortex-review/SKILL.md still returns a match
    - grep "Anti-Sycophancy" skills/cortex-review/SKILL.md still returns a match (section 0 unchanged)
    - grep "Engineering Lens" skills/cortex-review/SKILL.md still returns a match (section 2 unchanged)
    - grep "Eval Failure Check" skills/cortex-review/SKILL.md still returns a match (unchanged)
  </acceptance_criteria>
  <done>cortex-review SKILL.md has 2 autonomy gates: eval_validation (conditional) and compliance_verdict (conditional). Full-auto skips both. Supervised preserves all existing behavior. Engineering/security/YAGNI review lenses are unaffected.</done>
</task>

</tasks>

<verification>
After both tasks, verify:
1. cortex-spec has 4 gate annotations (reclarify as MANDATORY, plus 3 conditional)
2. cortex-review has 2 gate annotations (both conditional)
3. Both files reference decisions.md for skip logging
4. Both files reference the autonomy config resolution pattern
5. Existing non-gate functionality in each file is unchanged
6. Run: `grep -l "autonomy" skills/cortex-spec/SKILL.md skills/cortex-review/SKILL.md` returns both files
</verification>

<success_criteria>
- cortex-spec SKILL.md patched with 4 gates (reclarify mandatory + critical_uncertainty + evidence_backing + contract_approval conditional)
- cortex-review SKILL.md patched with 2 gates (eval_validation + compliance_verdict conditional)
- Gate names match exactly the names in templates/cortex/autonomy.json
- Each non-mandatory gate logs skips to docs/cortex/handoffs/decisions.md
- Reclarify gate is explicitly annotated as MANDATORY and cannot be skipped
- Supervised preset (or no config) preserves pre-patch behavior in both skills
- Full-auto preset auto-proceeds past all non-mandatory gates
</success_criteria>

<output>
After completion, create `.planning/phases/14-gate-patches/14-02-SUMMARY.md`
</output>
