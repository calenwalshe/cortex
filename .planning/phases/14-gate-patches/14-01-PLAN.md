---
phase: 14-gate-patches
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/cortex-clarify/SKILL.md
  - skills/cortex-research/SKILL.md
  - skills/cortex-audit/SKILL.md
autonomous: true
requirements: [AUTON-01, AUTON-02]

must_haves:
  truths:
    - "With preset full-auto, cortex-clarify slug conflict check auto-proceeds without prompting the user"
    - "With preset full-auto, cortex-research eval proposal approval auto-proceeds without blocking"
    - "With preset full-auto, cortex-audit security verdict auto-proceeds without blocking"
    - "With preset supervised (or no config), all three skills behave identically to pre-patch behavior"
    - "Mandatory gates (reclarify) still block even in full-auto mode"
  artifacts:
    - path: "skills/cortex-clarify/SKILL.md"
      provides: "Autonomy-aware slug_conflict gate"
      contains: "resolve-autonomy"
    - path: "skills/cortex-research/SKILL.md"
      provides: "Autonomy-aware eval_proposal gate"
      contains: "resolve-autonomy"
    - path: "skills/cortex-audit/SKILL.md"
      provides: "Autonomy-aware security_verdict gate"
      contains: "resolve-autonomy"
  key_links:
    - from: "skills/cortex-clarify/SKILL.md"
      to: "scripts/cortex/resolve-autonomy.js"
      via: "autonomy config resolution instruction"
      pattern: "resolve-autonomy"
    - from: "skills/cortex-research/SKILL.md"
      to: "scripts/cortex/resolve-autonomy.js"
      via: "autonomy config resolution instruction"
      pattern: "resolve-autonomy"
    - from: "skills/cortex-audit/SKILL.md"
      to: "scripts/cortex/resolve-autonomy.js"
      via: "autonomy config resolution instruction"
      pattern: "resolve-autonomy"
---

<objective>
Patch cortex-clarify, cortex-research, and cortex-audit SKILL.md files with conditional gate wrappers that read the resolved autonomy config and either enforce or skip each gate based on the resolved boolean value.

Purpose: These three skills each have one gate to wrap (slug_conflict, eval_proposal, security_verdict). After this plan, preset: "full-auto" auto-proceeds past these gates while preset: "supervised" preserves existing behavior.

Output: Three patched SKILL.md files with autonomy-conditional gate instructions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-gate-patches/14-CONTEXT.md
@.planning/phases/13-autonomy-config-foundation/13-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From scripts/cortex/resolve-autonomy.js:
```javascript
// CLI mode: echo '{"projectConfig": {...}}' | node scripts/cortex/resolve-autonomy.js
// Returns: {"preset":"supervised","gates":{"slug_conflict":true,"eval_proposal":true,...}}

// Module mode:
const { resolveAutonomy, PRESET_DEFAULTS, MANDATORY_GATES } = require('./scripts/cortex/resolve-autonomy.js');
const { preset, gates } = resolveAutonomy({ projectConfig, globalConfig, invocationFlags });
// gates.slug_conflict → boolean
// gates.eval_proposal → boolean
// gates.security_verdict → boolean
```

From templates/cortex/autonomy.json (gate names that map to skills in this plan):
```json
{
  "gates": {
    "slug_conflict": true,       // cortex-clarify Phase 2
    "eval_proposal": true,       // cortex-research Phase 3b
    "security_verdict": true     // cortex-audit (confidence gate)
  }
}
```

Gate wrapper pattern (to be inserted as SKILL.md instructions):
```
**Autonomy gate check (`{gate_name}`):**
1. Resolve the autonomy config: read `.cortex/autonomy.json` (project) and `~/.claude/cortex-autonomy.json` (global). Determine the active preset. Look up `gates.{gate_name}` in the resolved config. If no autonomy config exists, default to `supervised` preset (all gates true).
2. If `gates.{gate_name}` is `false`: skip this gate — auto-proceed without prompting. Log the skip to `docs/cortex/handoffs/decisions.md` as: `| {ISO timestamp} | {gate_name} | auto-skipped | autonomy: {preset} |`
3. If `gates.{gate_name}` is `true`: enforce the gate as described below (existing behavior).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch cortex-clarify with slug_conflict gate wrapper</name>
  <files>skills/cortex-clarify/SKILL.md</files>
  <read_first>
    - skills/cortex-clarify/SKILL.md
    - scripts/cortex/resolve-autonomy.js
    - templates/cortex/autonomy.json
  </read_first>
  <action>
In `skills/cortex-clarify/SKILL.md`, Phase 2 ("Check prerequisite state"), the slug conflict gate is at the bullet point that reads:

```
- If `slug` field is already set to a **different** active slug, warn the user and ask to confirm before overwriting the active context.
```

Insert an autonomy gate check block BEFORE the existing three bullet points in Phase 2. The new text goes after the line "Read `.cortex/state.json`." and before the first bullet. The inserted block:

```markdown
**Autonomy gate check (`slug_conflict`):**
Before evaluating slug conflict, resolve the autonomy config:
1. Read `.cortex/autonomy.json` (project-level) and `~/.claude/cortex-autonomy.json` (global-level) if they exist.
2. Determine the active preset (default: `supervised` if no config found).
3. Look up `gates.slug_conflict` in the resolved config. Resolution order: invocation flags > project config > global config > preset defaults. Mandatory gates (`ux_taste_eval`, `human_action`, `reclarify`) are always forced true regardless of config.
4. If `gates.slug_conflict` is `false`: **skip the slug conflict check entirely** — auto-proceed without warning or asking confirmation. Log the skip by appending a row to `docs/cortex/handoffs/decisions.md`: `| {ISO timestamp} | slug_conflict | auto-skipped | autonomy: {preset} |`.
5. If `gates.slug_conflict` is `true` (or no autonomy config exists): evaluate the slug conflict check as described below (existing behavior preserved).
```

Then modify the existing slug-conflict bullet to be under a sub-heading or clearly conditional on the gate being active. Change:

```
- If `slug` field is already set to a **different** active slug, warn the user and ask to confirm before overwriting the active context.
```

To:

```
- If `slug` field is already set to a **different** active slug AND the `slug_conflict` gate is active (per autonomy check above): warn the user and ask to confirm before overwriting the active context.
```

Do NOT change the other two bullets ("If the file does not exist..." and "If `slug` matches...").
  </action>
  <verify>
    <automated>grep -c "slug_conflict" skills/cortex-clarify/SKILL.md | grep -q "^[2-9]" && grep -c "resolve-autonomy\|autonomy config\|autonomy gate" skills/cortex-clarify/SKILL.md | grep -q "^[1-9]" && grep -q "auto-skipped" skills/cortex-clarify/SKILL.md && grep -q "decisions.md" skills/cortex-clarify/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Autonomy gate check" skills/cortex-clarify/SKILL.md returns a match
    - grep "slug_conflict" skills/cortex-clarify/SKILL.md returns at least 2 matches (gate name in wrapper + conditional in bullet)
    - grep "auto-skipped" skills/cortex-clarify/SKILL.md returns a match
    - grep "decisions.md" skills/cortex-clarify/SKILL.md returns a match
    - grep "preset defaults" skills/cortex-clarify/SKILL.md returns a match (resolution order documented)
    - The existing "If the file does not exist" and "If slug matches" bullets are UNCHANGED
  </acceptance_criteria>
  <done>cortex-clarify SKILL.md has an autonomy-conditional slug_conflict gate that auto-skips when gate is false and preserves existing behavior when gate is true</done>
</task>

<task type="auto">
  <name>Task 2: Patch cortex-research with eval_proposal gate wrapper</name>
  <files>skills/cortex-research/SKILL.md</files>
  <read_first>
    - skills/cortex-research/SKILL.md
    - scripts/cortex/resolve-autonomy.js
    - templates/cortex/autonomy.json
  </read_first>
  <action>
In `skills/cortex-research/SKILL.md`, Phase 3b ("Write Eval Plan"), the eval proposal approval gate is the decision logic block starting at "If `approval_required: true` AND `Approval Status:` is NOT `approved`:" which outputs "BLOCKED: Eval Plan Cannot Be Written".

Insert an autonomy gate check block at the start of Phase 3b, AFTER the "**Trigger:**" paragraph and BEFORE the "**Prerequisites:**" heading. The inserted block:

```markdown
**Autonomy gate check (`eval_proposal`):**
Before checking eval proposal approval status, resolve the autonomy config:
1. Read `.cortex/autonomy.json` (project-level) and `~/.claude/cortex-autonomy.json` (global-level) if they exist.
2. Determine the active preset (default: `supervised` if no config found).
3. Look up `gates.eval_proposal` in the resolved config. Resolution order: invocation flags > project config > global config > preset defaults. Mandatory gates (`ux_taste_eval`, `human_action`, `reclarify`) are always forced true regardless of config.
4. If `gates.eval_proposal` is `false`: **skip the approval status check** — proceed directly to writing the eval plan as if `Approval Status: approved`. Log the skip by appending a row to `docs/cortex/handoffs/decisions.md`: `| {ISO timestamp} | eval_proposal | auto-skipped | autonomy: {preset} |`. Continue to the "If `approval_required: false` OR `Approval Status: approved`:" branch below.
5. If `gates.eval_proposal` is `true` (or no autonomy config exists): evaluate the approval status check as described below (existing behavior preserved — blocks when approval is pending).
```

Then modify the decision logic block to reference the autonomy check. Change the opening condition from:

```
If `approval_required: true` AND `Approval Status:` is NOT `approved`:
```

To:

```
If the `eval_proposal` gate is active (per autonomy check above) AND `approval_required: true` AND `Approval Status:` is NOT `approved`:
```

Do NOT modify the "If `Approval Status:` is `rejected`:" block (that always fires regardless of autonomy — a rejection is an explicit human decision, not a gate).
Do NOT modify the "If `approval_required: false` OR `Approval Status: approved`:" block.
  </action>
  <verify>
    <automated>grep -c "eval_proposal" skills/cortex-research/SKILL.md | grep -q "^[2-9]" && grep -c "autonomy" skills/cortex-research/SKILL.md | grep -q "^[1-9]" && grep -q "auto-skipped" skills/cortex-research/SKILL.md && grep -q "decisions.md" skills/cortex-research/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Autonomy gate check" skills/cortex-research/SKILL.md returns a match
    - grep "eval_proposal" skills/cortex-research/SKILL.md returns at least 2 matches
    - grep "auto-skipped" skills/cortex-research/SKILL.md returns a match
    - grep "decisions.md" skills/cortex-research/SKILL.md returns a match
    - The "Approval Status: rejected" block is UNCHANGED (grep for "BLOCKED: Eval proposal was rejected" still returns a match)
    - The "approval_required: false OR Approval Status: approved" block is UNCHANGED
  </acceptance_criteria>
  <done>cortex-research SKILL.md has an autonomy-conditional eval_proposal gate that auto-skips approval blocking when gate is false and preserves existing blocking behavior when gate is true</done>
</task>

<task type="auto">
  <name>Task 3: Patch cortex-audit with security_verdict gate wrapper</name>
  <files>skills/cortex-audit/SKILL.md</files>
  <read_first>
    - skills/cortex-audit/SKILL.md
    - scripts/cortex/resolve-autonomy.js
    - templates/cortex/autonomy.json
  </read_first>
  <action>
In `skills/cortex-audit/SKILL.md`, the security verdict gate maps to the "Confidence Gate" section at the end of the file. The security_verdict gate controls whether the audit findings require human review before the audit is considered actionable.

Insert an autonomy gate check section BEFORE the "## Confidence Gate" section (i.e., after the "Store Results" section's last line `Audit artifact written: docs/cortex/audits/{slug}/{timestamp}.md`). Add a new section:

```markdown
## Autonomy Gate: Security Verdict

**Autonomy gate check (`security_verdict`):**
After producing the Security Posture Report, resolve the autonomy config to determine if human review of the findings is required:
1. Read `.cortex/autonomy.json` (project-level) and `~/.claude/cortex-autonomy.json` (global-level) if they exist.
2. Determine the active preset (default: `supervised` if no config found).
3. Look up `gates.security_verdict` in the resolved config. Resolution order: invocation flags > project config > global config > preset defaults. Mandatory gates (`ux_taste_eval`, `human_action`, `reclarify`) are always forced true regardless of config.
4. If `gates.security_verdict` is `false`: **auto-proceed** — the audit artifact is written and the pipeline continues without pausing for human review of findings. Log the skip by appending a row to `docs/cortex/handoffs/decisions.md`: `| {ISO timestamp} | security_verdict | auto-skipped | autonomy: {preset} |`. Update `next_action` in `docs/cortex/handoffs/current-state.md` to reflect the auto-proceed: include the text "Security audit auto-proceeded (autonomy: {preset})".
5. If `gates.security_verdict` is `true` (or no autonomy config exists): **require human review** — after writing the audit artifact, output a prompt asking the user to review findings before proceeding. Set `next_action` in `current-state.md` to: "Review security audit at {artifact_path} — CRITICAL/HIGH findings require human acknowledgment before proceeding."
```

Also add a note to the existing "Store Results" section, after "Update `docs/cortex/handoffs/current-state.md`:" bullets. Add the following bullet:

```markdown
- `next_action`: set based on the `security_verdict` autonomy gate check (see "Autonomy Gate: Security Verdict" section below)
```
  </action>
  <verify>
    <automated>grep -c "security_verdict" skills/cortex-audit/SKILL.md | grep -q "^[2-9]" && grep -c "autonomy" skills/cortex-audit/SKILL.md | grep -q "^[1-9]" && grep -q "auto-skipped" skills/cortex-audit/SKILL.md && grep -q "decisions.md" skills/cortex-audit/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Autonomy Gate: Security Verdict" skills/cortex-audit/SKILL.md returns a match
    - grep "security_verdict" skills/cortex-audit/SKILL.md returns at least 3 matches (section title + gate name in wrapper + conditional)
    - grep "auto-skipped" skills/cortex-audit/SKILL.md returns a match
    - grep "decisions.md" skills/cortex-audit/SKILL.md returns a match
    - The "Confidence Gate" section is UNCHANGED (grep "Daily mode" and "Comprehensive mode" still return matches)
    - The "Severity Guide" section is UNCHANGED
    - The OWASP and STRIDE sections are UNCHANGED
  </acceptance_criteria>
  <done>cortex-audit SKILL.md has an autonomy-conditional security_verdict gate that auto-proceeds when gate is false and requires human review when gate is true</done>
</task>

</tasks>

<verification>
After all three tasks, verify:
1. Each patched file contains "Autonomy gate check" or "Autonomy Gate" text
2. Each patched file references "decisions.md" for skip logging
3. Each patched file references the autonomy config resolution pattern
4. Existing functionality (non-gate sections) in each file is unchanged
5. Run: `grep -l "autonomy" skills/cortex-clarify/SKILL.md skills/cortex-research/SKILL.md skills/cortex-audit/SKILL.md` returns all 3 files
</verification>

<success_criteria>
- Three SKILL.md files patched with autonomy-conditional gates
- Gate names match exactly: slug_conflict, eval_proposal, security_verdict
- Each gate logs skips to docs/cortex/handoffs/decisions.md
- Supervised preset (or no config) preserves pre-patch behavior in all three skills
- Full-auto preset auto-proceeds past all three gates
</success_criteria>

<output>
After completion, create `.planning/phases/14-gate-patches/14-01-SUMMARY.md`
</output>
