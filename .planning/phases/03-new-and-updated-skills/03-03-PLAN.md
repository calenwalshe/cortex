---
phase: 03-new-and-updated-skills
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/cortex-investigate/SKILL.md
  - skills/cortex-review/SKILL.md
  - skills/cortex-audit/SKILL.md
autonomous: true
requirements:
  - CMD-04
  - CMD-05
  - CMD-06
must_haves:
  truths:
    - "/cortex-investigate writes investigation artifact to docs/cortex/investigations/<slug>/<timestamp>.md"
    - "cortex-investigate optionally writes repair contract to docs/cortex/contracts/<slug>/contract-NNN.md"
    - "cortex-investigate updates current-state.md after writing artifact"
    - "/cortex-review writes review artifact to docs/cortex/reviews/<slug>/<timestamp>.md with a contract compliance section"
    - "The contract compliance section reads active_contract_path from current-state.md and checks done criteria"
    - "cortex-review updates current-state.md after writing artifact"
    - "/cortex-audit writes Security Posture Report to docs/cortex/audits/<slug>/<timestamp>.md"
    - "The audit artifact covers all 7 required lenses: auth, data handling, secrets, unsafe tools, input validation, deps, misuse"
    - "cortex-audit updates current-state.md after writing artifact"
    - "All three skills derive slug from <subject>/<target> argument when no active slug is set in state.json"
  artifacts:
    - path: "skills/cortex-investigate/SKILL.md"
      provides: "Updated skill: cortex-investigate with artifact writing added"
      min_lines: 80
    - path: "skills/cortex-review/SKILL.md"
      provides: "Updated skill: cortex-review with artifact writing and contract compliance added"
      min_lines: 80
    - path: "skills/cortex-audit/SKILL.md"
      provides: "Updated skill: cortex-audit with artifact writing added"
      min_lines: 90
  key_links:
    - from: "skills/cortex-investigate/SKILL.md"
      to: "docs/cortex/investigations/<slug>/"
      via: "output to file step (new terminal section)"
      pattern: "docs/cortex/investigations"
    - from: "skills/cortex-review/SKILL.md"
      to: "docs/cortex/reviews/<slug>/"
      via: "output to file step (new terminal section)"
      pattern: "docs/cortex/reviews"
    - from: "skills/cortex-review/SKILL.md"
      to: "active_contract_path in current-state.md"
      via: "contract compliance section reads active contract"
      pattern: "active_contract"
    - from: "skills/cortex-audit/SKILL.md"
      to: "docs/cortex/audits/<slug>/"
      via: "output to file step (new terminal section)"
      pattern: "docs/cortex/audits"
---

<objective>
Extend skills/cortex-investigate/SKILL.md, skills/cortex-review/SKILL.md, and skills/cortex-audit/SKILL.md with artifact writing. All three have strong existing protocols that must be fully preserved — only add a new terminal "Output to File" section.

Purpose: These three post-execution commands currently produce chat-only output. The vNext requirement is that all command outputs are repo-local artifacts so they survive /clear. Adding artifact writing as a terminal section satisfies this without weakening the protocols.

Output: Three updated SKILL.md files. Existing behavioral content intact; each gains a new artifact write step and current-state.md update.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/agent/projects/cortex/.planning/PROJECT.md
@/home/agent/projects/cortex/.planning/ROADMAP.md
@/home/agent/projects/cortex/.planning/phases/03-new-and-updated-skills/03-RESEARCH.md
@/home/agent/projects/cortex/docs/COMMANDS.md

<interfaces>
<!-- CRITICAL PRESERVATION RULE (from 03-RESEARCH.md Pitfall 2):
Do NOT trim, summarize, or compress existing protocol content when adding artifact writing.
Add artifact-writing as a new terminal section ("Output to File" or "Store Results") AFTER the existing output format block.
The Iron Law (investigate), multi-lens review (review), and CSO audit phases (audit) must be 100% preserved.
-->

<!-- Artifact path conventions:
investigations: docs/cortex/investigations/{slug}/{timestamp}.md
reviews:        docs/cortex/reviews/{slug}/{timestamp}.md
audits:         docs/cortex/audits/{slug}/{timestamp}.md
repair contract: docs/cortex/contracts/{slug}/contract-NNN.md (increment from existing contracts)
Timestamp format: YYYYMMDDTHHMMSSZ (filesystem-safe compact ISO UTC)
-->

<!-- Slug resolution rule for investigate/review/audit (from 03-RESEARCH.md Open Question 1):
1. Read .cortex/state.json — if slug is set and non-null, use it
2. If slug is null AND <subject>/<target> argument is provided: slugify the argument using the standard rule
   (lowercase, replace spaces/non-alphanumeric with hyphens, collapse consecutive hyphens, strip leading/trailing)
3. If slug is null AND no argument: block with "No active slug found. Run /cortex-clarify first to establish project context."
-->

<!-- current-state.md fields to update after any artifact write:
- recent_artifacts: append the new artifact path
- blockers: update if investigation/review/audit surfaced a hard blocker
- next_action: update with recommended next step
Mode field should NOT be changed by these skills — they are post-execution tools, mode stays as-is.
-->

<!-- cortex-review: contract compliance section
- Read active_contract_path from docs/cortex/handoffs/current-state.md
- If active_contract_path is set: read the contract file and extract done_criteria and validators
- Add a "Contract Compliance" section to the review report before writing the file:
  - For each done criterion: [PASS|FAIL|PARTIAL] — specific finding
  - For each validator: did it pass or not?
  - Overall compliance verdict: COMPLIANT | NON-COMPLIANT | PARTIALLY COMPLIANT
- If no active contract: note "No active contract found — contract compliance check skipped"
-->

<!-- cortex-audit required 7 lenses (from REQUIREMENTS.md CMD-06 and COMMANDS.md):
1. Authentication (matches OWASP Broken Access Control + Auth Failures)
2. Data handling (matches OWASP Cryptographic Failures + Data Integrity)
3. Secrets exposure (matches Phase 2 Secrets Archaeology in existing skill)
4. Unsafe tool usage (matches Phase 4 OWASP Insecure Design — no direct OWASP equivalent, internal concept)
5. Input validation (matches OWASP Injection)
6. Dependency risks (matches Phase 3 Dependency Supply Chain in existing skill)
7. Misuse vectors (matches STRIDE threat model in existing skill)
These map well to the existing skill's phases — the "Store Results" section only needs to verify all 7 are documented, not add new lenses.
-->

<!-- cortex-investigate: optional repair contract
The repair contract is optional — only write it when the investigation determines a repair loop is needed.
Contract path: docs/cortex/contracts/{slug}/contract-NNN.md
Increment NNN by scanning existing contracts in docs/cortex/contracts/{slug}/ and taking max + 1.
The human imports the repair contract explicitly — do not call GSD commands.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend skills/cortex-investigate/SKILL.md with artifact writing</name>
  <files>skills/cortex-investigate/SKILL.md</files>
  <action>
Read the existing skills/cortex-investigate/SKILL.md in full before making any changes. Preserve every line of existing content exactly — The Iron Law, all 5 phases, the Red Flags list, the Integration section. Do not trim or rephrase.

Add the following new content in this order:

1. **Update Arguments section** (add before the existing Arguments section or update if present):
   - `/cortex-investigate [<subject>]` — subject is what to investigate; defaults to current active contract context

2. **Add Phase 0: Resolve slug** (insert as a new phase before the existing Phase 1):
   - Read .cortex/state.json for the active slug
   - If slug is set (non-null): use it as the output directory slug
   - If slug is null and <subject> argument is provided: derive slug from <subject> using standard slugification rule (lowercase, hyphens, collapse/strip)
   - If slug is null and no argument: block with "No active slug found. Run /cortex-clarify first."

3. **Add new terminal section "Store Results"** (after Phase 5: Structured Report, before Integration):
   After the DEBUG REPORT is produced, write it to a repo-local artifact:
   - Timestamp: YYYYMMDDTHHMMSSZ
   - Target path: docs/cortex/investigations/{slug}/{timestamp}.md
   - Create directory if needed: mkdir -p docs/cortex/investigations/{slug}/
   - Write the full DEBUG REPORT block to the file
   - If the investigation concludes that a repair loop is needed (Status: DONE_WITH_CONCERNS or BLOCKED), optionally write a repair contract:
     - Scan docs/cortex/contracts/{slug}/ for existing contracts; increment counter for NNN
     - Contract path: docs/cortex/contracts/{slug}/contract-NNN.md
     - Create directory: mkdir -p docs/cortex/contracts/{slug}/
     - Populate templates/cortex/contract.md with repair scope derived from investigation findings
     - Note: "Human must import repair contract into GSD explicitly — do not run GSD commands"
   - Update docs/cortex/handoffs/current-state.md:
     - recent_artifacts: append docs/cortex/investigations/{slug}/{timestamp}.md (and contract path if written)
     - blockers: set if Status is BLOCKED, clear if DONE
     - next_action: reflect repair recommendation or "Investigation complete — no repair needed"
   - Update .cortex/state.json:
     - Append investigation artifact path (and contract path if written) to artifacts array

Output a final confirmation line after storing:
`Investigation artifact written: docs/cortex/investigations/{slug}/{timestamp}.md`
  </action>
  <verify>
    <automated>grep -c "docs/cortex/investigations" /home/agent/projects/cortex/skills/cortex-investigate/SKILL.md && grep -c "current-state.md" /home/agent/projects/cortex/skills/cortex-investigate/SKILL.md && grep -c "Iron Law" /home/agent/projects/cortex/skills/cortex-investigate/SKILL.md && grep -c "Phase 1" /home/agent/projects/cortex/skills/cortex-investigate/SKILL.md</automated>
  </verify>
  <done>skills/cortex-investigate/SKILL.md: (1) "docs/cortex/investigations" appears in artifact write step, (2) current-state.md update step present, (3) "Iron Law" and "Phase 1" still present (original protocol preserved), (4) optional repair contract write step with GSD prohibition note, (5) slug resolution logic handles null-slug edge case.</done>
</task>

<task type="auto">
  <name>Task 2: Extend skills/cortex-review/SKILL.md with artifact writing and contract compliance</name>
  <files>skills/cortex-review/SKILL.md</files>
  <action>
Read the existing skills/cortex-review/SKILL.md in full before making any changes. Preserve every line — the anti-sycophancy rules, all 6 numbered instruction sections (Identify What to Review, Engineering Lens, Security Lens, YAGNI Lens, Output Format, Handling Pushback), and the existing arguments section. Do not trim or rephrase.

Add the following new content:

1. **Update User-invocable trigger phrases** (add to existing Also trigger when list):
   - "review against contract", "contract review", "compliance review"

2. **Update Arguments section** — add the new primary form alongside existing:
   - `/cortex-review [<target>]` — file, PR, or component to review; defaults to current active contract scope

3. **Add Phase 0: Resolve slug and load contract** (insert as a new numbered section before the existing "1. Identify What to Review"):
   - Read .cortex/state.json for the active slug
   - If slug is null and <target> argument is provided: derive slug from <target> using standard slugification rule
   - If slug is null and no argument: proceed with slug as "unknown" (review can still run, but artifact path will use "unknown" as slug)
   - Read docs/cortex/handoffs/current-state.md to get active_contract_path
   - If active_contract_path is set: read the contract file and load done_criteria and validators for the compliance check

4. **Add new section "Contract Compliance"** (insert between the existing Output Format and Handling Pushback sections):
   After producing the CODE REVIEW output block, add a "Contract Compliance" section:
   - If active contract was loaded:
     - For each done criterion in the contract: evaluate against the review findings and mark [PASS|FAIL|PARTIAL] with a specific finding note
     - For each validator in the contract: note whether it would pass based on review findings
     - State an overall compliance verdict: COMPLIANT | NON-COMPLIANT | PARTIALLY COMPLIANT
     - Append this section to the CODE REVIEW output block before the closing delimiter
   - If no active contract found:
     - Note "Contract compliance: No active contract found — compliance check skipped"
   This section is required per COMMANDS.md — it cannot be omitted.

5. **Add new terminal section "Store Results"** (after Handling Pushback, at the end):
   After the CODE REVIEW block (including contract compliance section) is produced:
   - Timestamp: YYYYMMDDTHHMMSSZ
   - Slug: use resolved slug from Phase 0
   - Target path: docs/cortex/reviews/{slug}/{timestamp}.md
   - Create directory: mkdir -p docs/cortex/reviews/{slug}/
   - Write the full CODE REVIEW block (including contract compliance section) to the file
   - Update docs/cortex/handoffs/current-state.md:
     - recent_artifacts: append docs/cortex/reviews/{slug}/{timestamp}.md
     - blockers: set if review verdict is REQUEST CHANGES with blocking issues
     - next_action: reflect review verdict and contract compliance outcome
   - Update .cortex/state.json:
     - Append review artifact path to artifacts array
   - Output confirmation: `Review artifact written: docs/cortex/reviews/{slug}/{timestamp}.md`

**Important:** Output is always written as a repo-local artifact. Chat-only review responses do not satisfy this command.
  </action>
  <verify>
    <automated>grep -c "docs/cortex/reviews" /home/agent/projects/cortex/skills/cortex-review/SKILL.md && grep -c "active_contract" /home/agent/projects/cortex/skills/cortex-review/SKILL.md && grep -c "current-state.md" /home/agent/projects/cortex/skills/cortex-review/SKILL.md && grep -c "Anti-Sycophancy" /home/agent/projects/cortex/skills/cortex-review/SKILL.md</automated>
  </verify>
  <done>skills/cortex-review/SKILL.md: (1) "docs/cortex/reviews" in artifact write step, (2) active_contract referenced in contract compliance section, (3) current-state.md update step present, (4) "Anti-Sycophancy" or equivalent anti-sycophancy content preserved from original, (5) contract compliance section includes PASS/FAIL/PARTIAL per criterion and overall COMPLIANT/NON-COMPLIANT verdict, (6) note that chat-only responses do not satisfy the command.</done>
</task>

<task type="auto">
  <name>Task 3: Extend skills/cortex-audit/SKILL.md with artifact writing</name>
  <files>skills/cortex-audit/SKILL.md</files>
  <action>
Read the existing skills/cortex-audit/SKILL.md in full before making any changes. Preserve every line — the CSO persona, all 7 phases (Phase 0 through Phase 6 plus Confidence Gate and Severity Guide), the "You do NOT make code changes" rule, and the existing arguments section. Do not trim or rephrase.

Add the following new content:

1. **Update Arguments section** — add the new primary form alongside existing:
   - `/cortex-audit [<target>]` — scope to audit; defaults to current active contract write roots

2. **Add Phase -1: Resolve slug** (insert before Phase 0: Stack Detection, renumber or label clearly):
   - Read .cortex/state.json for the active slug
   - If slug is set (non-null): use it as the output directory slug
   - If slug is null and <target> argument is provided: derive slug from <target> using standard slugification rule (lowercase, hyphens)
   - If slug is null and no argument: proceed with slug as "unknown" (audit can still run; artifact path will use "unknown" as slug)

3. **Add new terminal section "Store Results"** (after Phase 6: Security Posture Report and Confidence Gate, before the Severity Guide):
   After the SECURITY POSTURE REPORT is produced:
   - Verify that all 7 required lenses are covered in the report. For any lens with no findings, add an explicit note: "No issues found under [lens name]" — silence on a lens is not acceptable.
   - The 7 required lenses to verify:
     1. Authentication (from Phase 4 OWASP: Broken Access Control + Auth Failures)
     2. Data handling (from Phase 4 OWASP: Cryptographic Failures + Data Integrity)
     3. Secrets exposure (from Phase 2: Secrets Archaeology)
     4. Unsafe tool usage (from Phase 4 OWASP: Insecure Design)
     5. Input validation (from Phase 4 OWASP: Injection)
     6. Dependency risks (from Phase 3: Supply Chain)
     7. Misuse vectors (from Phase 5: STRIDE threat model)
   - Timestamp: YYYYMMDDTHHMMSSZ
   - Slug: use resolved slug
   - Target path: docs/cortex/audits/{slug}/{timestamp}.md
   - Create directory: mkdir -p docs/cortex/audits/{slug}/
   - Write the full SECURITY POSTURE REPORT block (with all 7 lenses documented) to the file
   - Update docs/cortex/handoffs/current-state.md:
     - recent_artifacts: append docs/cortex/audits/{slug}/{timestamp}.md
     - blockers: set if any CRITICAL findings discovered
     - next_action: reflect highest-severity findings or "Audit complete — no critical findings"
   - Update .cortex/state.json:
     - Append audit artifact path to artifacts array
   - Output confirmation: `Audit artifact written: docs/cortex/audits/{slug}/{timestamp}.md`

**Important:** Output is always a repo-local artifact. Chat-only audit responses do not count.
  </action>
  <verify>
    <automated>grep -c "docs/cortex/audits" /home/agent/projects/cortex/skills/cortex-audit/SKILL.md && grep -c "current-state.md" /home/agent/projects/cortex/skills/cortex-audit/SKILL.md && grep -c "Chief Security Officer\|CSO\|You do NOT make code changes" /home/agent/projects/cortex/skills/cortex-audit/SKILL.md && grep -c "Unsafe tool\|unsafe tool" /home/agent/projects/cortex/skills/cortex-audit/SKILL.md</automated>
  </verify>
  <done>skills/cortex-audit/SKILL.md: (1) "docs/cortex/audits" in artifact write step, (2) current-state.md update step present, (3) CSO persona and "do NOT make code changes" rule preserved from original, (4) all 7 required lenses listed explicitly in the store results section (including "unsafe tool usage"), (5) note that silence on a lens is not acceptable — every lens must have a documented finding or explicit no-issues note.</done>
</task>

</tasks>

<verification>
Run these checks after all three tasks complete:

```bash
# cortex-investigate: artifact write + protocol preserved
grep "docs/cortex/investigations" /home/agent/projects/cortex/skills/cortex-investigate/SKILL.md
grep "current-state.md" /home/agent/projects/cortex/skills/cortex-investigate/SKILL.md
grep "Iron Law" /home/agent/projects/cortex/skills/cortex-investigate/SKILL.md

# cortex-review: artifact write + contract compliance + anti-sycophancy preserved
grep "docs/cortex/reviews" /home/agent/projects/cortex/skills/cortex-review/SKILL.md
grep "active_contract" /home/agent/projects/cortex/skills/cortex-review/SKILL.md
grep "PASS\|FAIL\|PARTIAL\|COMPLIANT" /home/agent/projects/cortex/skills/cortex-review/SKILL.md

# cortex-audit: artifact write + 7 lenses + CSO persona preserved
grep "docs/cortex/audits" /home/agent/projects/cortex/skills/cortex-audit/SKILL.md
grep -i "unsafe tool" /home/agent/projects/cortex/skills/cortex-audit/SKILL.md
grep "You do NOT make code changes\|do not make code changes" /home/agent/projects/cortex/skills/cortex-audit/SKILL.md
```
</verification>

<success_criteria>
- skills/cortex-investigate/SKILL.md: Iron Law + 5 phases preserved, artifact write to docs/cortex/investigations/, optional repair contract write, current-state.md update, slug resolution for no-active-slug case
- skills/cortex-review/SKILL.md: anti-sycophancy rules preserved, all 6 protocol sections preserved, new contract compliance section (PASS/FAIL/PARTIAL per criterion, overall verdict), artifact write to docs/cortex/reviews/, current-state.md update
- skills/cortex-audit/SKILL.md: CSO persona preserved, all 7 phases preserved, "do NOT make code changes" rule preserved, all 7 required lenses verified in store step with no-silence rule, artifact write to docs/cortex/audits/, current-state.md update
</success_criteria>

<output>
After completion, create `.planning/phases/03-new-and-updated-skills/03-03-SUMMARY.md`
</output>
