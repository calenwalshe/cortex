# Eval Results: kalshi-calibration-harness

**Slug:** kalshi-calibration-harness
**Timestamp:** 20260407T060200Z
**Eval Plan:** docs/cortex/evals/kalshi-calibration-harness/eval-plan.md

---

## Results Summary

| Dimension | Result | Notes |
|-----------|--------|-------|
| Functional correctness | PASSED | 40/40 tests pass, all 4 external validators pass, all 10 done criteria met |
| Integration | PASSED | `from_config_file` round-trip works, records round-trip preserves all 21 fields, no SDK types in public interface |
| Safety/security | PASSED (with fix) | No credential material in source. `.gitignore` was missing key patterns — fixed: added `*.pem`, `*.key`, `*.p8`, `credentials.json`, `.env` |
| Style | PASSED | All 6 public APIs have docstrings, SDK isolation clean, config commented, tests organized by concern class |

**Overall: PASSED**

---

## Detailed Results

### Step 1: Import validators
```
from scripts.kalshi.kalshi_client import KalshiClient → import OK
from scripts.kalshi.records import BetRecord, write_record, read_records → import OK
```

### Step 2: Config validator
```
config OK (top-level 'api' key present)
```

### Step 3: Test suite
```
40 passed in 0.86s
- TestClientConstruction: 3/3
- TestMarketOperations: 3/3
- TestOrderOperations: 2/2
- TestPositionAndSettlement: 2/2
- TestConfigLoading: 3/3
- TestBetRecord: 3/3
- TestValidation: 15/15
- TestWriteAndRead: 9/9
```

### Step 4: Credential material scan
Grep matched only docstring examples (`api_key_id="..."`, `private_key_path=...`) — no actual secrets. PASSED.

### Step 5: .gitignore key patterns
FAILED initially — no key file patterns in `.gitignore`. Fixed by adding `*.pem`, `*.key`, `*.p8`, `credentials.json`, `.env`. Re-verified: PASSED.

### Step 6: Docstring check
All 6 public APIs (`KalshiClient`, `load_config`, `BetRecord`, `write_record`, `read_records`, `validate_record`) have docstrings. PASSED.

### Step 7: SDK type leak check
No `kalshi_python` references outside of internal import/init code. All public methods return `dict` or `list[dict]`. PASSED.

---

## Fixes Applied During Eval

1. Added key file patterns to `.gitignore`: `*.pem`, `*.key`, `*.p8`, `credentials.json`, `.env`
