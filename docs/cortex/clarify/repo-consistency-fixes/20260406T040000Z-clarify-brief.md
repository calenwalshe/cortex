# Clarify Brief: repo-consistency-fixes

<!-- ART-01: Clarify Brief Template — produced by /cortex-clarify -->

**Slug:** repo-consistency-fixes
**Timestamp:** 20260406T040000Z
**Status:** draft
**Complexity:** trivial

---

## Idea

Fix the remaining doc inconsistencies from the autonomous-builder-ideas.md Section A audit. Three items remain:

**A1 (STILL BROKEN):** COMMANDS.md only documents 8 commands but README.md and CORTEX.md say 13. Five commands missing from COMMANDS.md: cortex-bridge, cortex-close, cortex-fit, cortex-drive, cortex-stash. Also CORTEX.md has internal inconsistency — says 13 in header but "8-command reference" in file structure comment.

**A4 (PARTIALLY BROKEN):** COMMANDS.md says spec needs "one research dossier" but doesn't clarify eval-plan timing relative to spec approval. EVALS.md says eval plan required before done. The gate is deterministic but the docs don't make the sequencing clear.

**A5 (PARTIALLY BROKEN):** eval-plan.md template has a "## Results" section meant to be populated after running, but EVALS.md says results go to results-{timestamp}.md. Two locations for the same data — need one canonical location.

A2 (pricing.json path) and A3 (.planning/ exception) are already fixed.

---

## Goal

All Cortex documentation is internally consistent so an autonomous agent can follow the docs without hitting contradictions — command counts match across files, eval-plan timing is unambiguous, and eval results have one canonical location.

---

## Non-Goals

- Not rewriting or restructuring the docs — just fixing contradictions
- Not adding new commands or features
- Not changing any runtime behavior (hooks, scripts, state machine)
- Not touching SKILL.md files — those are the source of truth for each command's behavior

---

## Constraints

- Fixes must be documentation-only — no code changes
- The 13-command count in README.md and CORTEX.md header is correct (matches runtime-manifest.json) — COMMANDS.md and the CORTEX.md file-structure comment are wrong
- Eval-plan timing must match what EVALS.md already says: eval plan required before contract closes to `done`, not before spec
- Eval results canonical location must be one place, not two

---

## Assumptions

- The 13 commands listed in runtime-manifest.json are the ground truth for what's installed
- SKILL.md files for each command are the behavioral source of truth — COMMANDS.md is a reference summary
- The `## Results` section in eval-plan.md template is the stale/duplicate location (results-{timestamp}.md is canonical per EVALS.md)
- No other files beyond COMMANDS.md, CORTEX.md, and templates/cortex/eval-plan.md need changes for these three issues

---

## Open Questions

- Should COMMANDS.md document all 13 commands with the same depth, or should the 5 missing commands (bridge, close, fit, drive, stash) get lighter entries since they're utility/lifecycle commands rather than core pipeline commands?
- For A5: should the `## Results` section be removed from eval-plan.md entirely, or replaced with a pointer to results-{timestamp}.md?
- Are there other files beyond COMMANDS.md, CORTEX.md, and eval-plan.md template that reference the wrong command count or have the dual-results ambiguity?

---

## Next Research Steps

1. Grep for command count references ("8 command", "7 command") across all .md files to find any other stale counts
2. Check each of the 5 missing commands (bridge, close, fit, drive, stash) has a SKILL.md — confirm inputs/outputs/rules for COMMANDS.md entries
3. Grep for "results-" and "## Results" across templates and EVALS.md to map every location that references eval result storage
4. Verify COMMANDS.md /cortex-spec section against EVALS.md to draft the corrected eval-plan timing language
